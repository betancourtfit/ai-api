---
phase: quick-260724-p04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/response-normalizer.test.ts
  - tests/integration/server.test.ts
  - adapters/inbound/http/routes/transcriptions.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: false
requirements: [QUICK-260724-p04]

must_haves:
  truths:
    - "`bunx tsc --noEmit` exits 0 with zero errors (today: 6 errors)"
    - "`bun test` still reports 119 pass / 0 fail — no assertion, no test-name, no runtime-value changes"
    - "`bun run typecheck` exists as a package.json script and runs tsc --noEmit"
    - "Every push to master and every pull request runs a single `verify` job: install -> typecheck -> test -> secret-env guard"
    - "The guard fails the build if any .env* file outside the {.env.example, .env.test} allowlist is tracked in git"
    - "The whole job passes when simulated from a clean checkout containing only git-tracked/non-ignored files (no .env, no .env.local, no node_modules)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Single-job CI gate on push-to-master and pull_request"
      contains: "oven-sh/setup-bun@v2"
    - path: "package.json"
      provides: "typecheck script consumed by CI"
      contains: "\"typecheck\": \"tsc --noEmit\""
    - path: "adapters/inbound/http/routes/transcriptions.ts"
      provides: "FormData annotation derived from the call's own return type (removes TS2322)"
      contains: "Awaited<ReturnType<typeof request.formData>>"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts.typecheck"
      via: "bun run typecheck step"
      pattern: "bun run typecheck"
    - from: ".github/workflows/ci.yml"
      to: "bun.lock (text lockfile, lockfileVersion 1)"
      via: "setup-bun pinned to a version that can READ a text bun.lock"
      pattern: "bun-version:"
    - from: ".github/workflows/ci.yml"
      to: "git index"
      via: "git ls-files secret-env guard step"
      pattern: "git ls-files"
---

<objective>
Wire the non-negotiable CI minimum: make `bunx tsc --noEmit` green, expose it as a `typecheck` script, and add a single GitHub Actions job that runs install + typecheck + test + a tracked-secret-env guard on every push to master and every PR.

Purpose: The repo has zero CI today (no `.github/` directory). `bun test` is already green (119 pass, ~196ms, no network) but nothing enforces it, and `bunx tsc --noEmit` is red — so a type regression can reach master unnoticed. STATE.md already carries this as debt: "`bunx tsc --noEmit` reports 7 pre-existing errors and no typecheck gate is wired into CI."

Output: 6 type errors fixed with zero runtime change, a `typecheck` script, and `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@package.json
@Dockerfile

# LOCKED FACTS (verified this session by the planner — do NOT re-research)
#
# TSC: `bunx tsc --noEmit` reports exactly 6 errors, NOT 5, and they are NOT all in tests/.
#   The orchestrator brief said "5 errors, all in tests/, none in production code". That is wrong.
#   Verified list:
#     1. adapters/inbound/http/routes/transcriptions.ts(19,9)  TS2322  <-- PRODUCTION CODE
#     2. tests/integration/server.test.ts(288,17)              TS2352
#     3. tests/integration/server.test.ts(317,17)              TS2352
#     4. tests/unit/response-normalizer.test.ts(118,17)        TS2352
#     5. tests/unit/response-normalizer.test.ts(119,17)        TS2352
#     6. tests/unit/response-normalizer.test.ts(120,17)        TS2352
#   The production error MUST be fixed or the CI typecheck gate is red on run #1, which defeats
#   the entire point of this task. The fix is a one-line type ANNOTATION change with zero runtime
#   effect — it is not a production refactor.
#
# BUN VERSION: The repo lockfile is `bun.lock` (text format, lockfileVersion 1). There is no
#   bun.lockb. The text lockfile was introduced in Bun v1.1.39 and became default in v1.2
#   (verified against bun.com release notes + install/lockfile docs). Therefore the Dockerfile's
#   pinned `oven/bun:1.1.29` PREDATES the format and cannot read bun.lock. Pinning CI to 1.1.29
#   as originally briefed would make `bun install --frozen-lockfile` fail on run #1.
#   => CI pins `1.3.11` (the version that produced this lockfile and that the developer runs
#      locally). The resulting Dockerfile drift is a FOLLOW-UP NOTE in the SUMMARY, not a fix here
#      (docker changes are explicitly out of scope).
#
# TRACKED ENV FILES: `git ls-files -- '.env*'` returns exactly one path: `.env.test`.
#   It is intentionally tracked (Bun auto-loads .env.test under `bun test`, which is how CI gets
#   deterministic config with no repo secrets). The guard MUST allowlist it or CI is red on run #1.
#   Its CONTENTS are unverified — the planner could not read it (sandbox blocks .env* reads).
#   Task 3 makes the developer confirm it holds fixtures, not live keys.
#
# TYPESCRIPT AVAILABILITY: typescript@5.9.3 IS a resolved entry in bun.lock (via peerDependencies),
#   and node_modules/.bin/tsc exists. `bun install --frozen-lockfile` is therefore expected to
#   provide tsc in CI. Task 2's clean-checkout simulation proves this rather than assuming it.
#
# SCOPE FENCE (explicit developer instruction — "el minimo no negociable"):
#   OUT: GHCR / docker build / image push, EasyPanel deploy webhook, smoke tests, dependabot,
#        trivy, gitleaks, CodeQL, nightly provider contract tests, matrix builds, manual cache,
#        coverage, lint (no linter exists in this repo).
#   OUT: branch protection — that is GitHub UI configuration, not a repo change. Mention it as a
#        manual follow-up step in the SUMMARY. Do NOT create a task for it.
#   NOTE: because gitleaks is explicitly excluded, the guard is FILE-BASED ONLY (is a secret env
#        file tracked?). Do NOT add regex secret scanning of file contents.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `bunx tsc --noEmit` exit 0 and add the typecheck script</name>
  <files>tests/unit/response-normalizer.test.ts, tests/integration/server.test.ts, adapters/inbound/http/routes/transcriptions.ts, package.json</files>
  <action>
Fix all 6 type errors with the smallest possible edit. No test refactoring, no assertion changes, no renamed tests, no runtime-value changes anywhere.

(A) tests/unit/response-normalizer.test.ts lines 118, 119, 120 — each currently casts `(result as Record<string, unknown>)`. TS2352 fires because `ChatCompletionResult` has no index signature. On each of the three lines, insert `unknown as ` so the cast reads `(result as unknown as Record<string, unknown>)`. Change nothing else on those lines — the property keys (`time_info`, `x_groq`, `usage_breakdown`) and the `.toBeUndefined()` assertions stay exactly as-is. Do NOT touch lines 123 or 125 (`result.choices[0] as Record<string, unknown>` and `result.choices[0]?.message as Record<string, unknown>`) — they do not error.

(B) tests/integration/server.test.ts lines 288 and 317 — both read `new Headers({ 'retry-after': '30' }) as ConstructorParameters<typeof CerebrasAPIError>[3]`. The Cerebras SDK's `Headers` type is an index-signature record, so the DOM `Headers` instance is not assignable. On both lines, insert `unknown as ` so the cast reads `as unknown as ConstructorParameters<typeof CerebrasAPIError>[3]`. Do NOT replace `new Headers(...)` with a plain object literal: the runtime value must stay a real `Headers` instance because `adapters/outbound/sdk-error-mapper.ts` consumes it, and swapping it would change what these two cooldown tests actually exercise. Do NOT touch tests/domain/rate-limits.test.ts line 136 — it uses the same cast shape but does not error (its source value is already a record).

(C) adapters/inbound/http/routes/transcriptions.ts line 17 — the declaration `let formData: FormData;` triggers TS2322 because `request.formData()` returns the undici-typed FormData while the bare `FormData` identifier resolves to a different declaration in this lib setup. Replace the annotation with one derived from the call itself: `let formData: Awaited<ReturnType<typeof request.formData>>;` (`request` is already destructured from `ctx` on line 14, so it is in scope). `Awaited<ReturnType<Request['formData']>>` is an acceptable equivalent if the first form misbehaves. This is a type annotation only — zero runtime effect. Do NOT add a cast such as `as unknown as FormData` (it would lie about the type). Do NOT restructure the try/catch, do NOT touch line 19 or anything below it, and do NOT touch the `formData.entries()` loop.

(D) package.json — add `"typecheck": "tsc --noEmit"` to the `scripts` object, placed after the existing `"test"` entry. Do not reorder or modify `start`, `dev`, or `test`. Do not add any dependency; typescript@5.9.3 already resolves through bun.lock. Keep the file valid JSON with the existing 2-space indentation.

Confirm the total production diff is exactly one line (transcriptions.ts:17). If `bunx tsc --noEmit` surfaces any error not in the six listed in the context block, stop and report it rather than fixing it silently — an unexpected error means the toolchain moved and the developer needs to know.
  </action>
  <verify>
    <automated>bunx tsc --noEmit &amp;&amp; echo TYPECHECK_CLEAN</automated>
    <automated>bun test 2>&amp;1 | tail -5</automated>
    <automated>bun run typecheck &amp;&amp; echo SCRIPT_OK</automated>
    <automated>git diff --numstat -- adapters/inbound/http/routes/transcriptions.ts</automated>
  </verify>
  <done>
`bunx tsc --noEmit` prints nothing and exits 0 (TYPECHECK_CLEAN echoed). `bun test` reports 119 pass and 0 fail. `bun run typecheck` succeeds (proves the script resolves tsc). `git diff --numstat` on transcriptions.ts shows `1	1` — exactly one line added, one removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add .github/workflows/ci.yml and prove the job green from a clean checkout</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml`. There is no `.github/` directory yet — create the full path.

Workflow shape (exactly this, nothing more):
- `name: CI`
- Triggers: `on.push.branches` limited to `[master]`, plus a bare `on.pull_request` (so PRs targeting any branch are gated).
- Top-level `permissions:` with `contents: read` only. This is the least-privilege GITHUB_TOKEN and is what makes fork PRs safe to run (see the threat model below).
- A single job keyed `verify`, `runs-on: ubuntu-latest`. No matrix, no strategy, no services, no container, no manual cache step, no `env:` block, and no reference to `secrets.*` anywhere.

Steps, in this order:
1. `uses: actions/checkout@v4` — no `with:` inputs needed.
2. `uses: oven-sh/setup-bun@v2` with `bun-version: 1.3.11` (quoted string, so YAML cannot coerce it to a number). Pin 1.3.11, NOT the Dockerfile's 1.1.29 — the LOCKED FACTS block in the context section explains why 1.1.29 cannot read this repo's text `bun.lock`. Add a short YAML comment above the pin recording that reason so the next reader does not "helpfully" revert it to match the Dockerfile.
3. `name: Install` running `bun install --frozen-lockfile`.
4. `name: Typecheck` running `bun run typecheck`.
5. `name: Test` running `bun test`.
6. `name: Guard - no secret env files tracked` running a multi-line shell body that:
   - computes the offending list with `tracked="$(git ls-files | grep -E '(^|/)\.env($|\.)' | grep -vE '(^|/)\.env\.(example|test)$' || true)"` — the trailing `|| true` is required because a clean repo makes grep exit 1 and GitHub runs `run:` bodies under `bash -e`;
   - if `tracked` is non-empty, emits a `::error::` annotation line, prints the offending paths with `printf '%s\n' "$tracked"`, and `exit 1`;
   - otherwise echoes a single OK line that names the allowlist explicitly.
   The two regexes are anchored with `(^|/)` so the guard also catches a secret env file committed inside a subdirectory (e.g. `config/.env.local`), not just at the repo root.
   Add a YAML comment above this step stating that `.env.test` is deliberately allowlisted because it holds test fixtures and is what gives `bun test` deterministic config in CI, and that its contents were human-verified (Task 3). Print PATHS ONLY — never file contents — so a leaked value can never be echoed into a public CI log.

Do not add any other step, job, or trigger. Specifically: no docker build, no GHCR login, no deploy, no coverage upload, no lint, no dependabot config, no CodeQL, and no content-level secret scanning.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; grep -q 'oven-sh/setup-bun@v2' .github/workflows/ci.yml &amp;&amp; grep -q 'actions/checkout@v4' .github/workflows/ci.yml &amp;&amp; grep -q 'bun install --frozen-lockfile' .github/workflows/ci.yml &amp;&amp; grep -q 'bun run typecheck' .github/workflows/ci.yml &amp;&amp; grep -q 'contents: read' .github/workflows/ci.yml &amp;&amp; echo WORKFLOW_STRUCTURE_OK</automated>
    <automated>printf '.env\n.env.test\n.env.example\nconfig/.env.local\nindex.ts\nenvironment.ts\n' | grep -E '(^|/)\.env($|\.)' | grep -vE '(^|/)\.env\.(example|test)$'</automated>
    <automated>git ls-files | grep -E '(^|/)\.env($|\.)' | grep -vE '(^|/)\.env\.(example|test)$' || echo GUARD_CLEAN_ON_REAL_REPO</automated>
    <automated>TMP=$(mktemp -d) &amp;&amp; git ls-files -co --exclude-standard -z | rsync -0a --files-from=- ./ "$TMP/" &amp;&amp; (cd "$TMP" &amp;&amp; bun install --frozen-lockfile &amp;&amp; bun run typecheck &amp;&amp; bun test 2>&amp;1 | tail -5)</automated>
  </verify>
  <done>
WORKFLOW_STRUCTURE_OK is echoed. The synthetic-list check prints exactly two lines, `.env` and `config/.env.local` — proving the guard denies real secret files at any depth AND allows `.env.test` / `.env.example`. The real-repo check prints GUARD_CLEAN_ON_REAL_REPO (nothing tracked violates the guard today). The clean-checkout simulation — which contains only git-tracked and non-ignored files, so no `.env`, no `.env.local`, no `node_modules` — completes `bun install --frozen-lockfile`, `bun run typecheck`, and `bun test` with 119 pass / 0 fail. That last check is the real proof: it confirms the frozen lockfile installs, that the install provides `tsc`, and that the suite does not secretly depend on the untracked local `.env` files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — .env.test audit and first green Actions run</name>
  <action>
Blocking checkpoint. Present the what-built and how-to-verify blocks below to the developer and STOP. Do not push on the developer's behalf before step 1 (the `.env.test` audit) is answered — allowlisting a tracked env file in CI without confirming its contents would turn the secrets guard into theater.

On "approved": record the audit result and the Actions run status verbatim in the SUMMARY. On a reported failure: fix only what failed and re-present this checkpoint. Do not widen the guard allowlist and do not drop `--frozen-lockfile` to turn a red step green.
  </action>
  <what-built>
Six `tsc` errors fixed with zero runtime change (5 in tests, 1 one-line type annotation in `adapters/inbound/http/routes/transcriptions.ts`), a `typecheck` script in package.json, and `.github/workflows/ci.yml` running install + typecheck + test + a tracked-secret-env guard on push-to-master and pull requests. The job was simulated locally from a clean checkout and passed end to end.
  </what-built>
  <how-to-verify>
1. AUDIT `.env.test` (30 seconds, do this first). It is the only env file tracked in git, and the CI guard deliberately allowlists it. Open it and confirm every value is a placeholder/fixture — in particular that `CEREBRAS_API_KEY`, `GROQ_API_KEY`, and `PERSONAL_PROXY_API_KEY` are NOT live credentials (live Groq keys start `gsk_`, live Cerebras keys start `csk-`). The planner could not read this file, so this check is unautomated on purpose.
   - If the values are fixtures: say so and continue to step 2.
   - If ANY value is a real key: STOP. Do not push. Report it — the file needs rotation + history scrubbing, which is a separate task well outside this one.

2. PUSH AND WATCH THE RUN. Push the branch and open a PR against master (or push directly to master). Open the repo's Actions tab at github.com/betancourtfit/ai-api/actions and confirm the `verify` job goes green, with all four functional steps passing: Install, Typecheck, Test (119 pass), and the env guard (prints the OK line, not an `::error::`).

3. IF THE INSTALL STEP FAILS on lockfile format, report the exact error. The pin is `bun-version: "1.3.11"` because Bun 1.1.29 predates the text `bun.lock` format — if the runner still disagrees, the next move is `bun-version-file` or regenerating the lockfile, not silently dropping `--frozen-lockfile`.

4. IF THE GUARD STEP FAILS, read which path it printed. A path other than `.env.test` means a secret env file is genuinely tracked and the guard is doing its job — report it rather than widening the allowlist.
  </how-to-verify>
  <resume-signal>Type "approved" (with the .env.test audit result and the Actions run status), or describe what failed.</resume-signal>
  <done>
Developer confirmed `.env.test` holds fixture values only, and the `verify` job ran green on GitHub Actions with Install, Typecheck, Test (119 pass), and the env guard all passing. Both results are recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> GitHub Actions runner | Repo contents and third-party actions execute on Microsoft-hosted infrastructure |
| PR author -> runner | A fork PR can run arbitrary code from its own branch inside the `verify` job |
| developer workstation -> git history | Secret env files can be committed by accident; git history is permanent |
| runner -> public CI logs | Anything a step prints is world-readable if the repo is public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p04-01 | Tampering | `actions/checkout@v4`, `oven-sh/setup-bun@v2` referenced by mutable major tags | accept | Personal repo; job token is read-only and the job references zero `secrets.*`, so a compromised action gains no credentials and can only make the build lie about passing. SHA-pinning deferred as beyond the stated minimum. |
| T-p04-02 | Information Disclosure | Secret env files committed to the repo | mitigate | Guard step fails the build on any tracked path matching `(^|/)\.env($|\.)` outside the `{.env.example, .env.test}` allowlist, at any directory depth. The one allowlisted tracked file (`.env.test`) is human-audited in Task 3 before the allowlist is accepted. |
| T-p04-03 | Information Disclosure | Secret values echoed into public CI logs | mitigate | No step prints env values or file contents; the guard prints matched PATHS only. No `env:` block and no `secrets.*` reference exists anywhere in the workflow, so no secret is ever materialized on the runner. |
| T-p04-04 | Elevation of Privilege | Fork PR executing untrusted code | mitigate | Trigger is `pull_request` (NOT `pull_request_target`, which would run with the base repo's token and secrets), and top-level `permissions: contents: read` makes GITHUB_TOKEN read-only. A malicious fork PR gets a sandbox with no write access and no secrets. |
| T-p04-05 | Tampering | Dependency substitution during CI install | mitigate | `bun install --frozen-lockfile` installs exactly the pinned tree in `bun.lock`; the build fails rather than silently resolving a different version. |
| T-p04-SC | Tampering | npm/pip/cargo installs | N/A | Zero new packages are added by this plan. No `[ASSUMED]`/`[SUS]` package legitimacy checkpoint is required. |
</threat_model>

<verification>
- `bunx tsc --noEmit` exits 0 from a clean tree.
- `bun test` reports 119 pass / 0 fail — the same numbers as before this plan.
- `git diff --stat` shows changes confined to the five files in `files_modified`; production source is touched on exactly one line.
- The clean-checkout simulation (tracked + non-ignored files only) runs install -> typecheck -> test green, proving CI parity without `.env` / `.env.local` / `node_modules`.
- The guard regex pair denies `.env` and `config/.env.local` while allowing `.env.test` and `.env.example`.
- The real GitHub Actions `verify` run is green (Task 3, human-confirmed).
</verification>

<success_criteria>
- `bunx tsc --noEmit`: 6 errors -> 0.
- `package.json` exposes `typecheck`, and CI consumes it via `bun run typecheck`.
- `.github/workflows/ci.yml` exists with exactly one job (`verify`) and exactly the six steps specified.
- Pushing to master or opening a PR triggers a green run.
- Nothing from the excluded list (GHCR, docker build, deploy, dependabot, trivy, gitleaks, CodeQL, matrix, coverage, lint) appears in the workflow.
- No behavioral change to the proxy: same 119 tests, same assertions, same runtime values.
</success_criteria>

<output>
Create `.planning/quick/260724-p04-ci-minimo-fix-errores-tsc-en-tests-workf/260724-p04-SUMMARY.md` when done.

The SUMMARY must record these four follow-ups (none are in scope here):
1. **Branch protection is still off.** Require the `verify` check on master via GitHub Settings -> Branches -> Add rule. This is UI configuration, not a repo change.
2. **Dockerfile Bun drift.** `Dockerfile` pins `oven/bun:1.1.29`, which predates the text `bun.lock` format (introduced in Bun v1.1.39, default in v1.2). Its `RUN bun install --frozen-lockfile` is therefore at risk on a clean build; CI pins 1.3.11 instead. Worth reconciling before the next EasyPanel rebuild.
3. **STATE.md debt line is now stale.** "`bunx tsc --noEmit` reports 7 pre-existing errors and no typecheck gate is wired into CI" — it was 6 errors, all are fixed, and the gate now exists.
4. **`.env.test` is tracked and CI-allowlisted**, with its fixture-only status confirmed by human audit in Task 3. Record the audit result verbatim.
</output>
