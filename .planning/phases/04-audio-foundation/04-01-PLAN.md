---
phase: 04-audio-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.ts
  - types.ts
  - audio-schema.ts
  - tests/unit/audio-schema.test.ts
  - index.ts
autonomous: true
requirements: [AUDIO-01, AUDIO-02, AUDIO-03, AUDIO-04, AUDIO-05, AUDIO-06, WHSP-04, WHSP-05]

must_haves:
  truths:
    - "bun test passes with all existing v1.0 tests green and new audio-schema unit tests passing, with no whisper-server binary installed"
    - "An input object missing the file field fails validation with param 'file'"
    - "An input object with an unknown field (e.g. language) fails validation"
    - "A File larger than the configured limit is detected by the exported size validator (the 413 path)"
    - "response_format 'json' is accepted; any other response_format value is rejected"
    - "A missing WHISPER_MODEL_ALIAS env var leaves config.whisperModelAlias null without crashing import of config.ts"
  artifacts:
    - path: "audio-schema.ts"
      provides: "audioTranscriptionSchema, validateAudioTranscription, validateAudioFileSize, AudioTranscriptionInput"
      exports: ["audioTranscriptionSchema", "validateAudioTranscription", "validateAudioFileSize"]
      min_lines: 40
    - path: "tests/unit/audio-schema.test.ts"
      provides: "Unit tests for audio schema and size validator"
      min_lines: 60
    - path: "config.ts"
      provides: "whisperHost, whisperPort, whisperTimeoutMs, whisperModelAlias, audioMaxFileBytes, maxRequestBodyBytes"
      contains: "WHISPER_MODEL_ALIAS"
    - path: "types.ts"
      provides: "AudioTranscriptionResult interface"
      contains: "AudioTranscriptionResult"
    - path: "index.ts"
      provides: "maxRequestBodySize set from config; chat 1 MiB content-length gate"
      contains: "maxRequestBodySize"
  key_links:
    - from: "index.ts"
      to: "config.ts"
      via: "config.audioMaxFileBytes in Bun.serve options"
      pattern: "maxRequestBodySize:\\s*config\\.audioMaxFileBytes"
    - from: "index.ts"
      to: "config.ts"
      via: "config.maxRequestBodyBytes content-length check in chat handler"
      pattern: "config\\.maxRequestBodyBytes"
    - from: "tests/unit/audio-schema.test.ts"
      to: "audio-schema.ts"
      via: "import of validators"
      pattern: "from ['\"]\\.\\./\\.\\./audio-schema['\"]"
    - from: "audio-schema.ts"
      to: "zod"
      via: "z.strictObject schema"
      pattern: "z\\.strictObject"
---

<objective>
Lay the Phase 4 audio foundation: whisper/audio env vars in config, the `AudioTranscriptionResult` type, a Zod `audio-schema.ts` validator module with unit tests, and the `maxRequestBodySize` change in `Bun.serve()` with an explicit 1 MiB content-length gate for chat completions. No route handler is wired — Phase 5 does that. No whisper binary is required to run or test anything in this plan.

Purpose: Phase 5 can wire a `/v1/audio/transcriptions` route handler against pre-built, pre-tested contracts (schema, size validator, config fields, types) without renegotiating validation behavior.

Output: modified `config.ts`, `types.ts`, `index.ts`; new `audio-schema.ts` and `tests/unit/audio-schema.test.ts`; full `bun test` suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-audio-foundation/04-RESEARCH.md
@.planning/phases/04-audio-foundation/04-PATTERNS.md
@config.ts
@request-schema.ts
@types.ts

<interfaces>
<!-- Key existing contracts the executor uses directly — no exploration needed. -->

From config.ts (existing helper to reuse for WHISPER_MODEL_ALIAS):
```typescript
function optional(name: string): string | null;   // returns null when env var unset — no crash
export const config = { ... } as const;           // all process.env reads live here
```

From request-schema.ts (exact structural mirror for audio-schema.ts):
```typescript
export const chatCompletionSchema = z.strictObject({ ... });
export function validateChatCompletion(body: unknown):
    { success: true; data: ChatCompletionInput } |
    { success: false; param: string | null; message: string };
```

From index.ts (reuse as-is; do not redefine):
```typescript
function openaiError(message: string, type: string, code: string | number, param?: string | null, status?: number): Response;  // lines 29-40
export function createServer(adapters: Record<Provider, ProviderAdapter>, port?: number): ReturnType<typeof Bun.serve>;       // line 94 — signature MUST NOT change
function withRequestId(response: Response): Response;  // defined inside fetch() at line 108
// chat route entry: line 187 — if (request.method === 'POST' && pathname === '/v1/chat/completions')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add whisper and audio size config fields to config.ts</name>
  <files>config.ts</files>
  <read_first>config.ts</read_first>
  <action>
    Append six new fields inside the existing `config` object in config.ts, before the closing `} as const` (WHSP-04). Follow the existing patterns exactly: `Number(process.env["VAR"] ?? default)` for numerics, `process.env["VAR"] ?? "default"` for strings, and the existing `optional()` helper for nullable values. Fields and defaults:
    - `whisperHost`: string from `WHISPER_HOST`, default `"127.0.0.1"`
    - `whisperPort`: number from `WHISPER_PORT`, default `8080`
    - `whisperTimeoutMs`: number from `WHISPER_TIMEOUT_MS`, default `30_000`
    - `whisperModelAlias`: `optional("WHISPER_MODEL_ALIAS")` — MUST use the existing `optional()` helper so a missing env var yields `null` and never throws (phase success criterion 3)
    - `audioMaxFileBytes`: number from `AUDIO_MAX_FILE_BYTES`, default `26_214_400` (25 MiB) — AUDIO-03 / WHSP-05
    - `maxRequestBodyBytes`: number from `MAX_REQUEST_BODY_BYTES`, default `1_048_576` (1 MiB) — the chat-completion JSON body limit enforced at the validation layer in Task 3
    Do not add a second helper function, do not remove `as const`, do not touch existing fields, and do not read process.env anywhere outside config.ts.
  </action>
  <verify>
    <automated>bun test && grep -c "whisperModelAlias\|audioMaxFileBytes\|maxRequestBodyBytes\|whisperHost\|whisperPort\|whisperTimeoutMs" config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'optional("WHISPER_MODEL_ALIAS")' config.ts` matches exactly one line
    - `grep -n '26_214_400' config.ts` matches one line (audioMaxFileBytes default)
    - `grep -n '1_048_576' config.ts` matches one line (maxRequestBodyBytes default)
    - `WHISPER_MODEL_ALIAS= bun -e 'import { config } from "./config"; console.log(config.whisperModelAlias)'` prints `null` and exits 0 (missing alias does not crash)
    - `bun -e 'import { config } from "./config"; console.log(config.whisperPort, config.audioMaxFileBytes)'` prints `8080 26214400` when env vars are unset
    - `bun test` still reports all existing tests passing
  </acceptance_criteria>
  <done>config exports all six new fields with documented defaults; missing WHISPER_MODEL_ALIAS yields null; all existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create audio-schema.ts validator + AudioTranscriptionResult type + unit tests</name>
  <files>audio-schema.ts, types.ts, tests/unit/audio-schema.test.ts</files>
  <read_first>request-schema.ts, request-schema.test.ts, types.ts, tests/unit/response-normalizer.test.ts</read_first>
  <behavior>
    - Test 1: valid input `{ model: "whisper-1", file: new File(["audio data"], "t.mp3", { type: "audio/mpeg" }) }` returns `success: true` with `data.model === "whisper-1"` (AUDIO-01/02 happy path)
    - Test 2: input missing `file` returns `success: false` with `param === "file"` (AUDIO-01)
    - Test 3: input missing `model` returns `success: false` with `param === "model"` (AUDIO-02 structural half — alias lookup is Phase 5)
    - Test 4: input with unknown field `language: "en"` returns `success: false` (AUDIO-05)
    - Test 5: `response_format: "json"` returns `success: true`; `response_format: "text"` returns `success: false` (AUDIO-04 + AUDIO-05)
    - Test 6: `file` passed as a plain string returns `success: false` with `param === "file"` (FormData.get union — Pitfall 5)
    - Test 7: `validateAudioFileSize(file, maxBytes)` returns `{ ok: false }` with a message containing the byte limit when `file.size > maxBytes`, and `{ ok: true }` otherwise (AUDIO-03, the unit-testable 413 path)
    - Test 8: omitting `response_format` entirely returns `success: true` (optional field)
  </behavior>
  <action>
    Create `audio-schema.ts` at repo root as a structural mirror of `request-schema.ts` (RED first: write tests/unit/audio-schema.test.ts covering the behaviors above, run `bun test tests/unit/audio-schema.test.ts` and confirm failure, then implement).

    In `audio-schema.ts`:
    - Import zod with `import * as z from 'zod'` (matching request-schema.ts). Use `z.strictObject()` — NOT the Zod v3 `.strict()` method, which does not exist in v4.
    - Export `audioTranscriptionSchema = z.strictObject({...})` with exactly three fields: `model: z.string()` (AUDIO-02), `file: z.instanceof(File)` (AUDIO-01 — use `z.instanceof(File)`, not `z.file()`, per RESEARCH.md Pattern 2 note), `response_format: z.literal('json').optional()` (AUDIO-04; non-json values rejected via AUDIO-05).
    - Export `type AudioTranscriptionInput = z.infer<typeof audioTranscriptionSchema>`.
    - Export `validateAudioTranscription(input: unknown)` returning the same discriminated union shape as `validateChatCompletion` in request-schema.ts: `{ success: true; data: AudioTranscriptionInput } | { success: false; param: string | null; message: string }`. Copy the first-issue error-extraction logic from request-schema.ts lines 32-59 verbatim (path-based param, `unrecognized_keys` keys fallback, null fallback, 'Invalid request body' default message).
    - Export `validateAudioFileSize(file: File, maxBytes: number): { ok: true } | { ok: false; message: string }` — an explicit pre-Zod size check returning a failure when `file.size > maxBytes`, with the message including the maxBytes value. This is the unit-testable 413 path (AUDIO-03); do NOT use `z.file().max()` for this, since Zod size violations are 400-shaped ZodErrors, not 413s (RESEARCH.md Pitfall 1). The function takes maxBytes as a parameter (caller passes `config.audioMaxFileBytes`) so tests do not depend on env state.

    In `types.ts`: append `export interface AudioTranscriptionResult { text: string }` after the last existing interface (AUDIO-06 — the OpenAI json transcription response shape). Use `interface`, not a type alias, matching file convention. Do NOT add AudioTranscriptionInput to types.ts — it lives in audio-schema.ts via z.infer.

    In `tests/unit/audio-schema.test.ts`: import `{ test, expect, describe }` from `"bun:test"` and `{ validateAudioTranscription, validateAudioFileSize }` from `"../../audio-schema"`. Use `new File(["..."], "name.mp3", { type: "audio/mpeg" })` (global in Bun, no import). Structure as two describe blocks mirroring request-schema.test.ts: one for core validation, one for the hardened allowlist + size cases. No server, no FormData parsing, no network, no whisper binary.

    `verbatimModuleSyntax` note: if any file imports `AudioTranscriptionInput`, it must use `import type`.
  </action>
  <verify>
    <automated>bun test tests/unit/audio-schema.test.ts && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun test tests/unit/audio-schema.test.ts` reports at least 8 passing tests, 0 failures
    - `grep -c 'z.strictObject' audio-schema.ts` returns 1; `grep -c '\.strict()' audio-schema.ts` returns 0
    - `grep -n 'z.instanceof(File)' audio-schema.ts` matches one line
    - `grep -n "z.literal('json').optional()" audio-schema.ts` matches one line
    - `grep -n 'export function validateAudioFileSize' audio-schema.ts` matches one line
    - `grep -n 'export interface AudioTranscriptionResult' types.ts` matches one line and the interface body contains only `text: string`
    - `bun test` passes the full suite (existing v1.0 tests untouched and green) with no whisper binary installed
  </acceptance_criteria>
  <done>audio-schema.ts exports schema, type, validateAudioTranscription, and validateAudioFileSize; types.ts exports AudioTranscriptionResult; 8+ unit tests pass; full suite green without any sidecar.</done>
</task>

<task type="auto">
  <name>Task 3: Raise maxRequestBodySize in Bun.serve and gate chat completions at 1 MiB</name>
  <files>index.ts</files>
  <read_first>index.ts, config.ts</read_first>
  <action>
    Two surgical edits to `createServer()` in index.ts (WHSP-05). The `createServer(adapters, port?)` signature MUST NOT change — read both values from the imported `config` object (RESEARCH.md Pitfall 3; existing integration tests call createServer with the current signature).

    Edit 1 — Bun.serve options (around line 98): add `maxRequestBodySize: config.audioMaxFileBytes` alongside `hostname` and `port`. This raises the network-level gate to the 25 MiB audio ceiling (Bun's default is 128 MiB, so this tightens the global gate while making room for audio uploads).

    Edit 2 — chat completions handler (route entry at line 187, `POST /v1/chat/completions`): immediately after the route match and BEFORE the existing `request.json()` parse, add a content-length pre-check. Read `Number(request.headers.get('content-length') ?? 0)`; if it exceeds `config.maxRequestBodyBytes`, return `withRequestId(openaiError(...))` with status `413`, type `'invalid_request_error'`, code `'request_too_large'`, param `null`, and a message stating the maximum is `config.maxRequestBodyBytes` bytes. Reuse the existing `openaiError` (lines 29-40) and `withRequestId` (line 108) helpers — do not define new error helpers. `maxRequestBodySize` is global per Bun.serve with no per-route override, which is why the chat 1 MiB limit must live here in the handler.

    No new imports are needed — `config` is already imported at the top of index.ts.
  </action>
  <verify>
    <automated>bun test && grep -n "maxRequestBodySize: config.audioMaxFileBytes" index.ts && grep -n "config.maxRequestBodyBytes" index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'maxRequestBodySize: config.audioMaxFileBytes' index.ts` matches exactly one line inside the Bun.serve options object
    - `grep -n "request_too_large" index.ts` matches at least one line, and that return path uses status 413
    - `grep -n 'export function createServer' index.ts` shows the unchanged two-parameter signature `(adapters: Record<Provider, ProviderAdapter>, port: number = config.port)`
    - The content-length check appears between the `/v1/chat/completions` route match (line ~187) and the `request.json()` call — verified by reading the diff
    - `bun test` passes the full suite, including the existing integration tests in tests/integration/server.test.ts, proving the signature and existing 400/401 behaviors are unchanged
  </acceptance_criteria>
  <done>Bun.serve uses config.audioMaxFileBytes as maxRequestBodySize; chat completions return an OpenAI-shaped 413 when content-length exceeds config.maxRequestBodyBytes; createServer signature unchanged; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Bun.serve | Untrusted request bodies (JSON and, in Phase 5, multipart) cross here; body size and field content are attacker-controlled |
| env → config.ts | Deployment-controlled but unvalidated strings parsed into numbers/strings at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | DoS | Bun.serve global body gate | mitigate | Task 3 sets `maxRequestBodySize: config.audioMaxFileBytes` (25 MiB), tightening Bun's 128 MiB default so oversized uploads are rejected at the network layer before buffering |
| T-04-02 | DoS | /v1/chat/completions JSON path | mitigate | Task 3 adds explicit content-length check returning 413 at `config.maxRequestBodyBytes` (1 MiB) — chat bodies cannot ride the raised audio ceiling |
| T-04-03 | Tampering | audio-schema.ts (unknown field injection) | mitigate | Task 2 uses `z.strictObject()` — unrecognized keys rejected with 400-shaped validation failure (AUDIO-05), mirroring the proven chat allowlist |
| T-04-04 | Tampering | file field type confusion (string smuggled where File expected) | mitigate | Task 2 uses `z.instanceof(File)` — FormData.get's `string | File | null` union collapses to a clean validation failure for non-File values |
| T-04-05 | Information disclosure | error messages reflecting request content | mitigate | All error paths use existing `openaiError()` with fixed messages; validator messages come from Zod issue text, never echo file contents or filenames (AUTH2-02 groundwork) |
| T-04-06 | DoS | config numeric parsing (`Number()` of garbage env values → NaN) | accept | Deployment-controlled env vars; NaN limits would fail loudly in testing; adding startup validation is out of phase scope and existing config fields share the same accepted pattern |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase — RESEARCH.md Package Legitimacy Audit confirms no installs; zod 4.4.3 and bun-types already in node_modules |
</threat_model>

<verification>
- `bun test` — full suite passes with zero failures and no whisper-server binary on the machine (phase success criterion 1)
- `bun test tests/unit/audio-schema.test.ts` — covers missing file (400-shaped), unknown alias structural half (model string required), oversized file (413 path via validateAudioFileSize), unknown fields, response_format handling (phase success criterion 2)
- `WHISPER_MODEL_ALIAS= bun -e 'import { config } from "./config"; console.log(config.whisperHost, config.whisperPort, config.whisperTimeoutMs, config.audioMaxFileBytes, config.whisperModelAlias)'` — prints defaults and `null`, exits 0 (phase success criterion 3)
- `grep -n 'maxRequestBodySize: config.audioMaxFileBytes' index.ts` + integration suite green — raised gate without changing chat behavior (phase success criterion 4)
</verification>

<success_criteria>
- All 8 phase requirement IDs (AUDIO-01..06, WHSP-04, WHSP-05) have implementing artifacts: schema fields, size validator, result type, config fields, serve option, and chat gate
- Full `bun test` suite green with no whisper binary
- No new npm packages installed; no process.env reads outside config.ts; createServer signature unchanged
- Phase 5 can import `validateAudioTranscription`, `validateAudioFileSize`, `AudioTranscriptionInput`, `AudioTranscriptionResult`, and the six config fields without modification
</success_criteria>

<output>
Create `.planning/phases/04-audio-foundation/04-01-SUMMARY.md` when done
</output>
