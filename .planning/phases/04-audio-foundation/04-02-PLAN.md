---
phase: 04-audio-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.ts]
autonomous: true
requirements: [WHSP-05]
gap_closure: true
gaps_closed: [CR-01]

must_haves:
  truths:
    - "A chunked-transfer client (no Content-Length header) sending >1 MiB to /v1/chat/completions receives 413"
    - "A client sending Content-Length: abc (NaN header) with >1 MiB body receives 413"
    - "A client sending an understated Content-Length with >1 MiB actual body receives 413"
    - "A well-formed request under 1 MiB continues to receive a normal 200/completion response"
    - "All 76 existing integration and unit tests remain green after the fix"
  artifacts:
    - path: "index.ts"
      provides: "Chat body gate enforced on actual buffered bytes, not client-supplied header"
      contains: "Buffer.byteLength"
  key_links:
    - from: "index.ts POST /v1/chat/completions handler"
      to: "config.maxRequestBodyBytes"
      via: "Buffer.byteLength(raw) > config.maxRequestBodyBytes"
      pattern: "Buffer\\.byteLength\\(raw\\)"
---

<objective>
Close CR-01: the chat 1 MiB body gate trusts the client-supplied Content-Length header.
Three bypass paths allow non-cooperative clients to send up to 25 MiB to the chat route:
(1) chunked-encoding clients omit Content-Length — Number(null??0)=0 — check passes;
(2) Content-Length: abc yields NaN — NaN>limit is false — check passes;
(3) understated headers pass the pre-check while carrying more bytes.

Purpose: Enforce the 1 MiB chat limit on actual buffered bytes so the security guarantee
stated in success criterion 4 (WHSP-05) holds for all clients, cooperative or not.

Output: index.ts with the header pre-check replaced by a raw-body read + Buffer.byteLength
gate, and JSON.parse(raw) replacing request.json(). The createServer signature, all
integration tests, and the audio 25 MiB global gate are unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-audio-foundation/04-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace header-based chat body gate with buffered-byte enforcement</name>

  <files>index.ts, tests/integration/server.test.ts</files>

  <read_first>
    Read index.ts lines 188-210 — the current POST /v1/chat/completions handler entry point.
    The block to replace spans lines 190-208:
      - Lines 191-200: header-based contentLength check (three bypass paths)
      - Lines 203-208: body = await request.json() with catch block

    Key identifiers to preserve unchanged:
      - withRequestId(openaiError(...)) call shape at lines 193-199
      - openaiError message string: `Request body too large. Maximum is ${config.maxRequestBodyBytes} bytes.`
      - openaiError args: type='invalid_request_error', code='request_too_large', param=null, status=413
      - The catch block error message: 'Request body must be valid JSON.'
      - config.maxRequestBodyBytes (1 MiB default from config.ts)

    Read tests/integration/server.test.ts lines 1-80 to understand existing test helpers
    (post(), url(), validBody, PROXY_KEY) and test structure before adding new tests.
  </read_first>

  <behavior>
    New tests to add in tests/integration/server.test.ts inside the existing
    'Integration: contract tests' describe block:

    - TEST-13: chunked body >1 MiB returns 413
      Send POST /v1/chat/completions with Authorization header, Content-Type: application/json,
      NO Content-Length header, body = JSON string of length > config.maxRequestBodyBytes (1_048_576).
      Construct the oversized body as: JSON.stringify({ model: 'gpt-oss-120b-balanced',
      messages: [{ role: 'user', content: 'x'.repeat(1_048_577) }] })
      Expected: status 413, body.error.code === 'request_too_large'

    - TEST-14: malformed Content-Length header (NaN) with >1 MiB body returns 413
      Same oversized body as TEST-13, plus explicit header 'Content-Length': 'abc'
      Expected: status 413, body.error.code === 'request_too_large'

    - TEST-15: understated Content-Length with >1 MiB body returns 413
      Same oversized body as TEST-13, plus explicit header 'Content-Length': '1'
      Expected: status 413, body.error.code === 'request_too_large'

    Note: Bun's fetch() in bun:test does send actual bytes regardless of the Content-Length
    header value set by the test. The gate must read actual bytes (Buffer.byteLength) to
    catch TEST-13 through TEST-15.
  </behavior>

  <action>
    STEP 1 — Write the three failing tests (TEST-13, TEST-14, TEST-15) into
    tests/integration/server.test.ts. Add them after the last existing test in the
    'Integration: contract tests' describe block. Run `bun test tests/integration/server.test.ts`
    and confirm the three new tests FAIL (413 is not returned) while all prior tests pass.

    STEP 2 — Edit index.ts lines 190-208. Replace this block:

      // WHSP-05: enforce 1 MiB chat limit explicitly (maxRequestBodySize is set to audio ceiling)
      const contentLength = Number(request.headers.get('content-length') ?? 0);
      if (contentLength > config.maxRequestBodyBytes) {
          return withRequestId(openaiError(
              `Request body too large. Maximum is ${config.maxRequestBodyBytes} bytes.`,
              'invalid_request_error',
              'request_too_large',
              null,
              413
          ));
      }

      // Parse JSON body
      let body: unknown;
      try {
          body = await request.json();
      } catch {
          return withRequestId(openaiError('Request body must be valid JSON.', 'invalid_request_error', 'invalid_request_error', null, 400));
      }

    With this block:

      // WHSP-05: enforce 1 MiB chat limit on actual buffered bytes — header not trusted
      // Optional fast-fail on clearly valid numeric headers to avoid buffering obviously-large requests
      const declaredLength = Number(request.headers.get('content-length'));
      if (Number.isFinite(declaredLength) && declaredLength > config.maxRequestBodyBytes) {
          return withRequestId(openaiError(
              `Request body too large. Maximum is ${config.maxRequestBodyBytes} bytes.`,
              'invalid_request_error',
              'request_too_large',
              null,
              413
          ));
      }

      // Read raw body and enforce actual byte length — prevents chunked/NaN/understated bypass
      let raw: string;
      try {
          raw = await request.text();
      } catch {
          return withRequestId(openaiError('Failed to read request body.', 'invalid_request_error', 'invalid_request_error', null, 400));
      }
      if (Buffer.byteLength(raw) > config.maxRequestBodyBytes) {
          return withRequestId(openaiError(
              `Request body too large. Maximum is ${config.maxRequestBodyBytes} bytes.`,
              'invalid_request_error',
              'request_too_large',
              null,
              413
          ));
      }

      // Parse JSON body from already-buffered raw string
      let body: unknown;
      try {
          body = JSON.parse(raw);
      } catch {
          return withRequestId(openaiError('Request body must be valid JSON.', 'invalid_request_error', 'invalid_request_error', null, 400));
      }

    STEP 3 — Run `bun test` (full suite). All 76 prior tests must remain green; all three
    new tests must now pass. If any prior test regresses, inspect the diff — the only change
    is in the body-reading block between the header check and the Zod validation call.
    No other code in index.ts is touched.
  </action>

  <verify>
    <automated>
      # Confirm Buffer.byteLength gate is present
      grep -c "Buffer.byteLength(raw)" index.ts

      # Confirm request.json() is gone from the chat handler (replaced by JSON.parse)
      grep -n "request.json()" index.ts

      # Confirm Number.isFinite fast-fail is present
      grep -c "Number.isFinite(declaredLength)" index.ts

      # Full test suite — must be 79 pass (76 prior + 3 new), 0 fail
      bun test
    </automated>
  </verify>

  <done>
    - Buffer.byteLength(raw) > config.maxRequestBodyBytes gate exists in index.ts
    - request.json() no longer called in the chat handler; JSON.parse(raw) used instead
    - Number.isFinite(declaredLength) fast-fail present before the buffered-byte gate
    - bun test: 79 pass, 0 fail (or more if other suites add tests — zero failures required)
    - TEST-13, TEST-14, TEST-15 pass: chunked, NaN-header, and understated-header clients all receive 413
    - Normal requests under 1 MiB continue to succeed (existing TEST-01 through TEST-12 green)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /v1/chat/completions | HTTP request body and headers arrive from untrusted downstream client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | index.ts chat body gate | mitigate | Buffer.byteLength(raw) enforces limit on actual bytes; Number.isFinite guard prevents NaN fast-fail bypass; request.text() buffers before JSON.parse so no double-read |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run after plan execution:

```bash
# Gate present in source
grep -c "Buffer.byteLength(raw)" index.ts
# Expected: 1

# Fast-fail guard present
grep -c "Number.isFinite(declaredLength)" index.ts
# Expected: 1

# request.json() removed from chat handler
grep -n "request\.json()" index.ts
# Expected: no output (or only in comments)

# Full suite green
bun test
# Expected: all pass, 0 fail
```
</verification>

<success_criteria>
- Buffer.byteLength(raw) enforces the 1 MiB limit on actual body bytes for all clients
- Chunked-encoding clients (no Content-Length) exceeding 1 MiB receive 413
- Clients sending Content-Length: abc (NaN) with >1 MiB body receive 413
- Clients sending understated Content-Length with >1 MiB body receive 413
- All 76 prior tests (unit + integration) remain green
- Three new regression tests (TEST-13, TEST-14, TEST-15) are green
- WHSP-05 requirement satisfied: maxRequestBodySize raised to audio ceiling; chat 1 MiB limit enforced on actual bytes
- Phase 04 gap CR-01 closed
</success_criteria>

<output>
Create `.planning/phases/04-audio-foundation/04-02-SUMMARY.md` when done.
</output>
