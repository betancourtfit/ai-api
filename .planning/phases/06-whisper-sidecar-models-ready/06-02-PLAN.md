---
phase: 06-whisper-sidecar-models-ready
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - index.ts
  - tests/integration/server.test.ts
autonomous: true
requirements: [EP2-02, EP2-03]

must_haves:
  truths:
    - "GET /v1/models returns the WHISPER_MODEL_ALIAS id alongside chat aliases when config.whisperModelAlias is set"
    - "GET /ready includes a whisperAvailable boolean reflecting whisperService.health()"
    - "GET /ready chat fields (ready, mode, eligibleProviders, unavailableProviders) and HTTP status are unchanged by the whisper addition"
    - "The production entrypoint selects HttpWhisperService only when config.whisperModelAlias !== null, otherwise NoopWhisperService"
    - "createServer() signature and its NoopWhisperService default are unchanged (Phase 5 test-injection seam preserved)"
  artifacts:
    - path: "index.ts"
      provides: "whisper alias in /v1/models; whisperAvailable in /ready; import.meta.main service selection"
      contains: "whisperAvailable"
    - path: "tests/integration/server.test.ts"
      provides: "healthMock on MockWhisperService; EP2-02 /v1/models test; EP2-03 /ready true+false tests"
      contains: "whisperAvailable"
  key_links:
    - from: "index.ts GET /ready handler"
      to: "whisperService.health()"
      via: "await on the in-scope createServer param"
      pattern: "await whisperService.health\\(\\)"
    - from: "index.ts import.meta.main"
      to: "HttpWhisperService"
      via: "config.whisperModelAlias !== null selection passed as 3rd createServer arg"
      pattern: "new HttpWhisperService\\(\\)"
    - from: "index.ts GET /v1/models handler"
      to: "config.whisperModelAlias"
      via: "conditional data.push of the whisper alias"
      pattern: "config.whisperModelAlias"
---

<objective>
Surface the two read-only whisper signals and wire the real service at boot: append the whisper alias to `GET /v1/models` (EP2-02), add `whisperAvailable` to `GET /ready` (EP2-03), and select `HttpWhisperService` vs `NoopWhisperService` inline in `import.meta.main` based on `config.whisperModelAlias` (D-01/D-03). Add integration coverage for both endpoints.

Purpose: These are pure handler edits against existing seams — no route logic or chat readiness math changes. The audio route 503 path (WHSP-03) is already wired from Phase 5 and is left untouched.
Output: Modified `index.ts` + extended `tests/integration/server.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-whisper-sidecar-models-ready/06-CONTEXT.md
@.planning/phases/06-whisper-sidecar-models-ready/06-RESEARCH.md
@.planning/phases/06-whisper-sidecar-models-ready/06-PATTERNS.md
@.planning/phases/06-whisper-sidecar-models-ready/06-01-SUMMARY.md

<interfaces>
<!-- Contracts from 06-01 (Wave 1) and existing index.ts seams — extracted; no exploration needed. -->

From whisper-service.ts (after 06-01):
```typescript
export interface WhisperService {
    transcribe(file: File, modelAlias: string): Promise<AudioTranscriptionResult>;
    health(): Promise<boolean>;
}
export class NoopWhisperService implements WhisperService { /* health() => false */ }
export class HttpWhisperService implements WhisperService { /* constructor(opts = {}) */ }
```

Existing index.ts seams (verified line refs):
- createServer signature (104-109): `createServer(adapters, port = config.port, whisperService = new NoopWhisperService(), audioMaxFileBytes = config.audioMaxFileBytes)`.
- Existing value/type imports (14-15): `import { NoopWhisperService } from './whisper-service'` and `import type { WhisperService } from './whisper-service'`.
- GET /ready handler (133-154): computes `ready`, `mode`, `eligibleProviders`, `unavailableProviders`; returns `withRequestId(new Response(JSON.stringify({...}), { status: ready ? 200 : 503 }))`. The `fetch` handler is already `async`; `whisperService` is in scope.
- GET /v1/models handler (275-289): `data: listAliases().map((id) => ({ id, object: 'model', created: 0, owned_by: 'personal-proxy' }))` wrapped in `withRequestId`.
- import.meta.main (785-789): `const server = createServer({ cerebras: cerebrasAdapter, groq: groqAdapter });`
- listAliases() (model-registry.ts:25-28) returns chat aliases only — the whisper alias is NOT in the registry, hence the explicit append.

`.env.test` sets `WHISPER_MODEL_ALIAS=whisper-1`, so the integration server sees a configured alias.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire /v1/models alias, /ready whisperAvailable, and entrypoint selection in index.ts</name>
  <read_first>
    - index.ts (the file being modified — read the GET /ready handler 133-154, auth gate ~156, GET /v1/models 275-289, audio route try/catch 228-260 to confirm the 503 path is untouched, import.meta.main 785-789, imports 14-15)
    - .planning/phases/06-whisper-sidecar-models-ready/06-RESEARCH.md (Patterns 2, 3, 4; Anti-Patterns list)
    - .planning/phases/06-whisper-sidecar-models-ready/06-PATTERNS.md (the three index.ts analog sections + Shared Patterns: withRequestId, optional-config null-guard)
    - whisper-service.ts (after 06-01 — HttpWhisperService + NoopWhisperService + health())
    - config.ts (lines 61-66: whisperModelAlias null-guard)
    - model-registry.ts (lines 25-28: listAliases())
  </read_first>
  <action>
    Extend the value import on index.ts line 14 to `import { NoopWhisperService, HttpWhisperService } from './whisper-service'` (keep the `import type { WhisperService }` line as-is).

    GET /v1/models (EP2-02): convert the inline `data` map into a `const data = listAliases().map((id) => ({ id, object: 'model', created: 0, owned_by: 'personal-proxy' }))`. After it, when `config.whisperModelAlias !== null`, `data.push({ id: config.whisperModelAlias, object: 'model', created: 0, owned_by: 'personal-proxy' })`. Serialize `{ object: 'list', data }` inside the existing `withRequestId(new Response(...))`. Do NOT gate the append on sidecar health (configured = alias env set). Handler stays after the auth gate (~156).

    GET /ready (EP2-03): immediately before the existing `return`, add `const whisperAvailable = await whisperService.health();`. Add `whisperAvailable` as a sibling key in the serialized object: `{ ready, mode, eligibleProviders, unavailableProviders, whisperAvailable }`. Do NOT fold whisper into `ready`/`mode`/`eligibleProviders`/`unavailableProviders`; the HTTP `status` MUST remain `ready ? 200 : 503` (ROADMAP criterion 2). Keep the `withRequestId(...)` wrapper. Rely on `health()` being bounded + non-throwing (from 06-01) so `/ready` cannot hang or 500.

    import.meta.main (D-01/D-03): before the `createServer(...)` call, add `const whisperService = config.whisperModelAlias !== null ? new HttpWhisperService() : new NoopWhisperService();` and pass `whisperService` as the explicit THIRD argument to `createServer({ cerebras: cerebrasAdapter, groq: groqAdapter }, config.port, whisperService)`. Do NOT change `createServer`'s signature or move the selection inside it (preserves the Phase 5 test-injection seam).
  </action>
  <verify>
    <automated>bun test tests/integration/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `index.ts` imports `HttpWhisperService` from `./whisper-service`
    - `index.ts` contains `await whisperService.health()` inside the `/ready` handler
    - `index.ts` `/ready` serialized object includes a `whisperAvailable` key and still computes `ready`/`mode`/`eligibleProviders`/`unavailableProviders` exactly as before; status remains `ready ? 200 : 503`
    - `index.ts` `/v1/models` pushes `config.whisperModelAlias` into `data` only when `!== null`, with `owned_by: 'personal-proxy'`
    - `index.ts` `import.meta.main` contains `new HttpWhisperService()` selected on `config.whisperModelAlias !== null` and passes it as the 3rd `createServer` arg
    - The `createServer(...)` signature (index.ts ~104-109) and the audio route try/catch (~228-260) are unchanged
    - `bun test tests/integration/server.test.ts` exits 0 (existing cases still green pre-Task-2)
  </acceptance_criteria>
  <done>Both read endpoints expose whisper signals additively and the real service is selected at boot when the alias is set; chat readiness and the createServer seam are byte-for-byte preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for /v1/models alias and /ready whisperAvailable (EP2-02/EP2-03)</name>
  <read_first>
    - tests/integration/server.test.ts (the file being modified — MockWhisperService type 18, makeMockWhisperService 27-35, beforeAll 37-52, beforeEach 54-64; and an existing audio test for the auth-header + fetch(audioUrl(...)) helper style)
    - .planning/phases/06-whisper-sidecar-models-ready/06-RESEARCH.md (Pattern 6: health mock on MockWhisperService; Pitfall 4 = no whole-body /ready toEqual; Pitfall 6 = reset healthMock)
    - .planning/phases/06-whisper-sidecar-models-ready/06-PATTERNS.md (tests/integration/server.test.ts analog section)
    - index.ts (after Task 1 — the /ready and /v1/models handlers under test)
  </read_first>
  <action>
    Widen the `MockWhisperService` type (line ~18) to `WhisperService & { transcribeMock: ReturnType<typeof mock>; healthMock: ReturnType<typeof mock> }`. In `makeMockWhisperService()` add `const healthMock = mock(async (): Promise<boolean> => true)` and return `{ transcribe: transcribeMock, health: healthMock, transcribeMock, healthMock }`. In `beforeEach` add `mockWhisper.healthMock.mockReset()` followed by `mockWhisper.healthMock.mockImplementation(async () => true)` (avoid order-dependence, Pitfall 6).

    Add EP2-02 test: `GET` `audioUrl('/v1/models')` with the `Authorization: Bearer ${PROXY_KEY}` header against `audioServer`; parse JSON and assert `data` (array of `{ id }`) includes an entry whose `id === 'whisper-1'` (the `.env.test` WHISPER_MODEL_ALIAS) AND still includes the existing chat alias id(s).

    Add EP2-03 tests against `audioServer` (which holds `mockWhisper`): (a) `mockWhisper.healthMock.mockImplementationOnce(async () => true)` then `GET /ready` → assert `body.whisperAvailable === true`; (b) `mockWhisper.healthMock.mockImplementationOnce(async () => false)` then `GET /ready` → assert `body.whisperAvailable === false`. In BOTH assert chat fields are unchanged: `body.ready`, `body.mode`, `body.eligibleProviders`, `body.unavailableProviders` are present and the same regardless of the whisper branch, and the HTTP status is not changed by the whisper value. Use field-level assertions only — no whole-body `toEqual` (Pitfall 4). `/ready` is unauthenticated, so no auth header is required for those calls.
  </action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/server.test.ts` `MockWhisperService` type includes `healthMock`
    - `makeMockWhisperService()` returns a `health` mock and the `beforeEach` resets `healthMock`
    - An EP2-02 test asserts `GET /v1/models` `data[]` includes `id === 'whisper-1'` and the existing chat alias
    - Two EP2-03 tests assert `body.whisperAvailable` is `true` and `false` respectively, driven by `healthMock`
    - The EP2-03 tests assert `ready`/`mode`/`eligibleProviders`/`unavailableProviders` are unaffected by the whisper branch
    - No `/ready` test uses a whole-body `toEqual`
    - `bun test` (full suite) exits 0 with all prior tests still green
  </acceptance_criteria>
  <done>EP2-02 and EP2-03 are proven via the injected mock for both health branches, with chat readiness asserted unchanged; the full suite stays green with no real binary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → proxy `/v1/*` | Authenticated surface behind the existing Bearer auth gate (index.ts ~156) |
| client → proxy `/ready` | Intentionally public; exposes only booleans/enums, no secrets or sidecar internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Spoofing | GET /v1/models alias listing | mitigate | Handler sits after the auth gate; listing the whisper alias still requires a valid Bearer token (unchanged) |
| T-06-02 | Denial of Service | GET /ready → whisperService.health() | mitigate | Consumes the bounded (~2s) non-throwing `health()` from 06-01; `/ready` cannot hang or 500 on a down sidecar |
| T-06-05 | Information Disclosure | GET /ready public body | accept | Only booleans/enums exposed (`whisperAvailable`, `ready`, `mode`, provider name arrays); no secrets, hosts, or transcripts |
| T-06-06 | Tampering | chat readiness math (ready/mode) | mitigate | `whisperAvailable` is an additive sibling field; chat `ready`/`mode`/`status` computation is left byte-for-byte (ROADMAP criterion 2) and asserted unchanged in tests |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (ROADMAP criterion 4); no install tasks → slopcheck N/A |
</threat_model>

<verification>
- `bun test` full suite green (existing ~90 tests + new EP2-02/EP2-03 cases).
- `/ready` chat fields and HTTP status unchanged across both whisper health branches.
- `createServer()` signature and audio route 503 path untouched.
</verification>

<success_criteria>
- `GET /v1/models` returns the whisper alias when configured (criterion 1).
- `GET /ready` reports `whisperAvailable` true/false without affecting chat `mode` (criterion 2).
- Production boot selects the real client only when the alias is set; default Noop preserved otherwise.
</success_criteria>

<output>
Create `.planning/phases/06-whisper-sidecar-models-ready/06-02-SUMMARY.md` when done.
</output>
