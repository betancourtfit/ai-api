---
phase: 06-whisper-sidecar-models-ready
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - whisper-service.ts
  - tests/services/http-whisper-service.test.ts
autonomous: true
requirements: [WHSP-01, WHSP-02, WHSP-03]

must_haves:
  truths:
    - "HttpWhisperService.transcribe() POSTs multipart FormData to the sidecar /inference path and returns { text }"
    - "The logical model alias is NOT forwarded to the sidecar (sidecar owns its --model)"
    - "transcribe() throws on non-2xx / network error / timeout so the existing route maps it to an OpenAI-shaped 503"
    - "health() returns a bounded boolean, never throws, and returns false when the sidecar is unreachable"
    - "All sidecar communication uses Bun-native fetch() + FormData + AbortSignal.timeout — zero new npm packages"
  artifacts:
    - path: "whisper-service.ts"
      provides: "health() on WhisperService interface; NoopWhisperService.health(); HttpWhisperService class"
      contains: "class HttpWhisperService"
    - path: "tests/services/http-whisper-service.test.ts"
      provides: "Fake in-process Bun.serve sidecar tests for WHSP-01/02/03"
      contains: "Bun.serve"
  key_links:
    - from: "whisper-service.ts HttpWhisperService.transcribe()"
      to: "http://host:port/inference"
      via: "native fetch POST with FormData"
      pattern: "fetch\\(.*inferencePath"
    - from: "whisper-service.ts HttpWhisperService.health()"
      to: "http://host:port/health"
      via: "bounded fetch GET (AbortSignal.timeout)"
      pattern: "/health"
---

<objective>
Extend `whisper-service.ts` with a real HTTP client for the whisper-server sidecar: add a bounded `health()` method to the `WhisperService` interface, implement `NoopWhisperService.health()`, and add the new `HttpWhisperService` class that talks to the verified whisper.cpp contract (`POST /inference` → `{ "text": ... }`, `GET /health` 200/503).

Purpose: This is the only genuinely new code in Phase 6. Every consumer seam (route 503 path, `createServer` third param, config fields) already exists; this plan produces the client they depend on.
Output: Modified `whisper-service.ts` + new fake-sidecar test file. No new npm packages.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-whisper-sidecar-models-ready/06-CONTEXT.md
@.planning/phases/06-whisper-sidecar-models-ready/06-RESEARCH.md
@.planning/phases/06-whisper-sidecar-models-ready/06-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. No exploration required. -->

Current whisper-service.ts (the surface to extend):
```typescript
import type { AudioTranscriptionResult } from './types';
export interface WhisperService {
    transcribe(file: File, modelAlias: string): Promise<AudioTranscriptionResult>;
}
export class NoopWhisperService implements WhisperService {
    async transcribe(_file: File, _modelAlias: string): Promise<AudioTranscriptionResult> {
        throw new Error('WhisperService not configured. Set WHISPER_HOST and WHISPER_MODEL_ALIAS.');
    }
}
```

Config fields available as constructor defaults (config.ts:61-66) — no new env vars:
`config.whisperHost` (string, default "127.0.0.1"), `config.whisperPort` (number, default 8080), `config.whisperTimeoutMs` (number, default 30000), `config.whisperModelAlias` (string | null).

Return type (types.ts:57-60): `interface AudioTranscriptionResult { text: string }`.

Verified whisper.cpp sidecar contract (06-RESEARCH.md): native transcription endpoint `POST /inference` (multipart `file` required, `response_format` optional, default body `{ "text": "..." }`); health endpoint `GET /health` returns HTTP 200 `{"status":"ok"}` or HTTP 503 `{"status":"loading model"}` — `/health` is NOT under the inference path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add health() to WhisperService and implement HttpWhisperService</name>
  <read_first>
    - whisper-service.ts (the full file being modified — current interface + NoopWhisperService)
    - .planning/phases/06-whisper-sidecar-models-ready/06-RESEARCH.md (Pattern 1 full code; Decision 1 = `/inference` default; Decision 3 = 2000ms health cap; Assumption A1)
    - .planning/phases/06-whisper-sidecar-models-ready/06-PATTERNS.md (`whisper-service.ts` analog section: imports pattern, config fields, result type, AbortSignal precedent)
    - config.ts (lines 61-66: whisperHost, whisperPort, whisperTimeoutMs, whisperModelAlias; line 5-8 optional())
    - types.ts (lines 57-60: AudioTranscriptionResult)
    - services/cerebras.ts (lines 69-73: the only existing `signal: AbortSignal` network precedent)
  </read_first>
  <action>
    First verify Assumption A1: run `bun -e "console.log(typeof AbortSignal.timeout)"` and confirm output is `function`. If it prints `undefined`, fall back to a manual `AbortController` + `setTimeout(...)` cleared in a `finally` (note the deviation in the SUMMARY).

    Add a value import `import { config } from './config'` to `whisper-service.ts` (keep the existing `import type { AudioTranscriptionResult } from './types'` — respect `verbatimModuleSyntax`, Pitfall 5).

    Add `health(): Promise<boolean>` to the `WhisperService` interface. Implement `NoopWhisperService.health()` to return `false` (not configured → never available).

    Add a non-exported `HttpWhisperOptions` shape with optional `host`, `port`, `inferencePath`, `timeoutMs`, `healthTimeoutMs` fields.

    Add and export `class HttpWhisperService implements WhisperService`. Constructor accepts `opts: HttpWhisperOptions = {}` and computes: `baseUrl` from `opts.host ?? config.whisperHost` and `opts.port ?? config.whisperPort`; `inferencePath` = `opts.inferencePath ?? '/inference'` (Decision 1 native default); `timeoutMs` = `opts.timeoutMs ?? config.whisperTimeoutMs`; `healthTimeoutMs` = `opts.healthTimeoutMs ?? 2000` (Decision 3).

    `transcribe(file, _modelAlias)`: build `new FormData()`, `append('file', file, 'audio')` (generic filename — never derive a name for logging, AUTH2-02), `append('response_format', 'json')`. `fetch(baseUrl + inferencePath, { method: 'POST', body: form, signal: AbortSignal.timeout(this.timeoutMs) })`. On `!res.ok` throw `new Error('whisper-server responded ' + res.status)` (status code ONLY — no body, filename, or transcript in the message). On success read `body.text` from `await res.json()` and return `{ text: body.text ?? '' }`. Do NOT append the `model` alias (WHSP-02 — sidecar owns its `--model`).

    `health()`: `fetch(baseUrl + '/health', { signal: AbortSignal.timeout(this.healthTimeoutMs) })` and `return res.ok` (503 loading → `res.ok` false). Wrap in `try { ... } catch { return false }` so a refused/hung connection yields `false`, never a throw (Pitfall 2).

    Do NOT add any `console.log`/`log()` calls inside the service. Do NOT change `NoopWhisperService.transcribe()`.
  </action>
  <verify>
    <automated>bun -e "console.log(typeof AbortSignal.timeout)" && bunx tsc --noEmit 2>/dev/null || bun build whisper-service.ts --target=bun >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `bun -e "console.log(typeof AbortSignal.timeout)"` prints `function` (A1 confirmed) OR an AbortController fallback is implemented and noted
    - `whisper-service.ts` contains `health(): Promise<boolean>` in the `WhisperService` interface
    - `whisper-service.ts` contains `export class HttpWhisperService`
    - `whisper-service.ts` contains the literal default `'/inference'` and the literal `2000`
    - `whisper-service.ts` imports `config` as a value import and keeps `import type` for `AudioTranscriptionResult`
    - No `model`/`modelAlias` value is appended to the outbound FormData (grep shows no `append('model'` / `append("model"`)
    - No `console.log` or `log(` call exists inside `whisper-service.ts`
    - File type-checks / builds without error
  </acceptance_criteria>
  <done>HttpWhisperService and the health() interface method exist, build cleanly, forward only file bytes + response_format to /inference, and probe /health with a 2s bound that resolves to false on failure.</done>
</task>

<task type="auto">
  <name>Task 2: Fake-sidecar tests for HttpWhisperService (WHSP-01/02/03)</name>
  <read_first>
    - .planning/phases/06-whisper-sidecar-models-ready/06-RESEARCH.md (Pattern 5: full fake-sidecar test; Validation Architecture → Phase Requirements→Test Map)
    - .planning/phases/06-whisper-sidecar-models-ready/06-PATTERNS.md (`tests/services/http-whisper-service.test.ts (NEW)` section)
    - tests/integration/server.test.ts (lines 37-52: in-process Bun.serve boot with port 0 + server.port readback)
    - tests/unit/audio-schema.test.ts (lines 1-5: unit-test header/import style)
    - whisper-service.ts (the class under test, after Task 1)
  </read_first>
  <action>
    Create new file `tests/services/http-whisper-service.test.ts`. In `beforeAll`, start an in-process fake sidecar via `Bun.serve({ port: 0, fetch })` whose handler routes by `new URL(req.url).pathname`: `/health` → `Response.json({ status: 'ok' })`; `/inference` → first capture whether the multipart body contains a `model` field (read `await req.formData()` and assert `form.get('model')` is null for WHSP-02), then return `Response.json({ text: 'hello world' })`; any other path → `new Response('not found', { status: 404 })`. In `afterAll` call `sidecar.stop(true)`.

    Construct the service under test with `new HttpWhisperService({ host: '127.0.0.1', port: sidecar.port })`.

    Tests: (1) WHSP-01 — `transcribe(new File(['x'], 'a'), 'whisper-1')` resolves to `{ text: 'hello world' }`. (2) WHSP-02 — assert the fake sidecar observed NO `model` form field on the `/inference` request. (3) WHSP-03 — a service pointed at `inferencePath: '/missing'` (404) has `transcribe(...)` reject (`rejects.toThrow()`). (4) health true — `health()` resolves `true` when `/health` returns 200. (5) health false — a service at an unreachable port (e.g. `port: 1, healthTimeoutMs: 300`) resolves `health()` to `false` without throwing.

    Use only `bun:test` (`beforeAll`, `afterAll`, `test`, `expect`) and real `fetch`/`FormData` — no `fetch` global mocking, no whisper binary.
  </action>
  <verify>
    <automated>bun test tests/services/http-whisper-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/services/http-whisper-service.test.ts` exists and contains `Bun.serve`
    - `bun test tests/services/http-whisper-service.test.ts` exits 0 with all 5 tests passing
    - A test asserts the sidecar received no `model` form field (WHSP-02)
    - A test asserts `transcribe()` rejects on a non-2xx sidecar response (WHSP-03)
    - A test asserts `health()` returns `false` against an unreachable port without throwing
    - The test file contains no reference to a real whisper binary or external network host
  </acceptance_criteria>
  <done>Real fetch()/FormData behavior of HttpWhisperService is proven against an in-process fake sidecar; the no-binary `bun test` gate is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy → whisper-server sidecar | Outbound HTTP from the proxy process to a local sidecar; host/port are server-side operator config, never client-supplied |
| sidecar → proxy (response body) | Untrusted JSON body returned by the sidecar is parsed and only `body.text` is read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | HttpWhisperService.transcribe()/health() | mitigate | Error message carries status code only (`whisper-server responded <status>`); never include filename, file bytes, transcript, or response body; service performs no logging |
| T-06-02 | Denial of Service | HttpWhisperService.health() | mitigate | `AbortSignal.timeout(2000)` on the probe + `catch → false`; a hung sidecar cannot block the caller |
| T-06-03 | Tampering (SSRF) | outbound fetch target URL | accept | Host/port/path derive from server-side `config` (or test-only constructor opts), never from request input; not client-influenced |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (ROADMAP criterion 4); no install tasks → slopcheck N/A |
</threat_model>

<verification>
- `bun test tests/services/http-whisper-service.test.ts` passes (WHSP-01/02/03 + health true/false).
- `bun test` full suite stays green (no interface regression for existing TEST2-xx — `NoopWhisperService` still satisfies the widened interface).
- `AbortSignal.timeout` confirmed available (A1) or fallback documented.
</verification>

<success_criteria>
- `HttpWhisperService` exists in `whisper-service.ts`, talks to `/inference` and `/health`, throws on failure, and never forwards the model alias.
- `health()` is bounded (~2s) and returns `false` rather than throwing on an unreachable sidecar.
- Zero new npm packages added.
</success_criteria>

<output>
Create `.planning/phases/06-whisper-sidecar-models-ready/06-01-SUMMARY.md` when done.
</output>
