---
phase: 03-full-compliance-tests
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - index.ts
  - routing/cooldown-manager.ts
  - model-registry.ts
autonomous: true
requirements: [NORM-10, OBS-01, OBS-02, OBS-03, OBS-04, OBS-05]
tags: [observability, request-id, structured-logging, createServer, error-shape]

must_haves:
  truths:
    - "Every HTTP response from the proxy — /health, /ready, /v1/models, success, 400, 401, 404, 503, and streaming — carries an X-Request-ID header with a UUID"
    - "Importing index.ts does not bind a port; createServer(adapters, port) boots the server, and `bun index.ts` still serves on config.port exactly as before"
    - "All error responses including both 404 paths return { error: { message, type, code, param } } — never a flat text body"
    - "Upstream error messages pass through to clients wrapped in the OpenAI error shape, with any known upstream model ID in the text rewritten to the logical alias"
    - "Each chat completion request emits exactly one structured JSON log line containing requestId, timestamp, route, logicalAlias, provider, upstreamModelId, attempt, streaming, statusCode, latencyMs, and failoverReason — and never API keys, Authorization headers, prompts, response content, or reasoning"
    - "X-LLM-Provider header appears on success responses only when EXPOSE_PROVIDER_HEADER=true (default false)"
  artifacts:
    - path: "index.ts"
      provides: "createServer(adapters, port) factory seam (D-02); import.meta.main entrypoint guard; X-Request-ID on every path; log() helper with LOG_LEVEL gating; per-request structured log"
      exports: ["createServer"]
      contains: "import.meta.main"
    - path: "model-registry.ts"
      provides: "Reverse model-ID rewrite for D-07 error message de-leaking"
      exports: ["rewriteUpstreamModelIds"]
    - path: "routing/cooldown-manager.ts"
      provides: "classifyError exposes the upstream error message for D-07 passthrough"
      contains: "message"
  key_links:
    - from: "index.ts"
      to: "model-registry.ts"
      via: "rewriteUpstreamModelIds applied to upstream error text before openaiError"
      pattern: "rewriteUpstreamModelIds\\("
    - from: "index.ts"
      to: "routing/cooldown-manager.ts"
      via: "classified.message consumed in non-failover error paths"
      pattern: "classified\\.message"
    - from: "tests (Plan 03-03)"
      to: "index.ts"
      via: "createServer export is the integration test seam"
      pattern: "export function createServer"
---

<objective>
Refactor `index.ts` into the `createServer(adapters)` factory (D-02), attach `X-Request-ID` to every response path (OBS-01), add the conditional `X-LLM-Provider` header (OBS-05), sweep all error paths into the OpenAI error shape including both flat 404s (NORM-10), pass upstream error messages through with model-ID de-leaking (D-07), and emit the full structured request log (OBS-02..04) plus the D-08 usage-missing warning.

Purpose: Satisfies Phase 3 success criteria 2 and 4, and creates the test seam Plan 03-03 requires.
Output: A testable, observable server factory with complete error-shape and header compliance.
</objective>

## Phase Goal

**As an** API consumer, **I want to** receive fully OpenAI-shaped, traceable responses from a proxy whose behavior is proven by a deterministic test suite, **so that** my existing OpenAI clients work unchanged and regressions are caught before they reach me.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-compliance-tests/03-CONTEXT.md
@.planning/phases/03-full-compliance-tests/03-PATTERNS.md
@.planning/phases/03-full-compliance-tests/03-01-SUMMARY.md
@index.ts
@routing/cooldown-manager.ts
@model-registry.ts
</context>

<interfaces>
<!-- Contracts this plan creates; Plan 03-03 builds against them. -->

```typescript
// index.ts — new export (D-02). Server type: ReturnType<typeof Bun.serve>.
export function createServer(
    adapters: Record<Provider, ProviderAdapter>,
    port?: number   // defaults to config.port; tests pass 0
): ReturnType<typeof Bun.serve>;

// model-registry.ts — new export (D-07)
export function rewriteUpstreamModelIds(text: string): string;

// routing/cooldown-manager.ts — widened return (D-07)
export function classifyError(err: unknown): {
    shouldFailover: boolean;
    status: number | undefined;
    headers: HeaderSource | undefined;
    message: string | undefined;   // NEW: err.message for SDK APIError instances
};
```

Existing exports consumed (from routing/provider-state.ts, unchanged): `chooseEligibleProviders`, `advanceCursor`, `setCooldown`, `recordSuccess`, `recordFailure`, `resetForTesting`, `getStateSnapshot`, `isEligible`, `setRateLimitSnapshot`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract createServer(adapters, port) factory with import.meta.main entrypoint guard</name>
  <files>index.ts</files>
  <read_first>index.ts, types.ts, .planning/phases/03-full-compliance-tests/03-PATTERNS.md</read_first>
  <action>
    Wrap the existing module-level `Bun.serve({...})` call (per D-02) in an exported factory: `export function createServer(adapters: Record<Provider, ProviderAdapter>, port: number = config.port)` returning the `Bun.serve` result. Inside the fetch handler, replace every reference to the module-level `adapterMap` const with the `adapters` parameter, then delete the `adapterMap` const entirely. `hostname` stays `config.hostname`.

    Entrypoint guard — IMPORTANT correction to RESEARCH Pattern 1: a bare top-level call still executes on import in Bun/ESM. Use Bun's `import.meta.main` instead: at the bottom of index.ts, write `if (import.meta.main) { const server = createServer({ cerebras: cerebrasAdapter, groq: groqAdapter }); console.log(\`Server is running on ${server.url}\`); }`. This keeps `bun index.ts` behavior identical while making `import { createServer } from './index'` side-effect-free regarding port binding (Plan 03-03 depends on this).

    Keep all handler logic, helpers (openaiError, extractBearerToken, verifyToken, hasVisibleChunkData, parseRateLimitHeaders, toRateLimitSnapshot), and route order byte-identical apart from the adapters parameter substitution — header/logging changes come in Tasks 2 and 3.
  </action>
  <verify>
    <automated>bun test && bun -e "const m = await import('./index.ts'); if (typeof m.createServer !== 'function') { console.error('createServer not exported'); process.exit(1); } console.log('import side-effect free, createServer exported');"</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains `export function createServer(` and `if (import.meta.main)`
    - `grep -v '^\s*//' index.ts | grep -c 'adapterMap'` outputs 0
    - `bun -e "await import('./index.ts')"` exits 0 without binding a port (no EADDRINUSE possible when run twice; no "Server is running" line printed)
    - `bun test` exits 0
  </acceptance_criteria>
  <done>createServer is the only place Bun.serve is called; importing index.ts is side-effect-free for ports; direct `bun index.ts` boots identically.</done>
</task>

<task type="auto">
  <name>Task 2: X-Request-ID on every path, X-LLM-Provider conditional, NORM-10 sweep, D-07 error passthrough</name>
  <files>index.ts, routing/cooldown-manager.ts, model-registry.ts</files>
  <read_first>index.ts, routing/cooldown-manager.ts, model-registry.ts, .planning/phases/03-full-compliance-tests/03-PATTERNS.md</read_first>
  <action>
    OBS-01 — request ID on EVERY response. At the very top of the fetch handler, before any branching: `const requestId = crypto.randomUUID();`. Define inside the handler a `withRequestId(response: Response): Response` helper that REBUILDS the response (always safe regardless of Bun header mutability — RESEARCH Open Question 1): construct `new Headers(response.headers)`, `set('X-Request-ID', requestId)`, return `new Response(response.body, { status: response.status, headers })`. Apply `withRequestId(...)` at every return site in the handler: /health (line ~94), /ready, auth-not-configured 503, auth 401, /internal/providers/status (both branches), /v1/models, JSON-parse 400, Zod 400, unknown-alias 400, no-provider 503 (both streaming and non-streaming), upstream-error returns, non-streaming success, and the 404 catch-all. EXCEPTION — the streaming success Response: headers are committed at construction for SSE, so add `'X-Request-ID': requestId` directly into its headers object (alongside Content-Type/Cache-Control/Connection), not via the wrapper.

    OBS-05 — on the two SUCCESS responses only (streaming and non-streaming completion), when `config.exposeProviderHeader` is true add header `'X-LLM-Provider'` with the chosen provider name (`cerebras` or `groq`). Use conditional spread in the headers object for streaming; for non-streaming, set it on the headers before wrapping or include it in the constructed Response headers. Default (false) emits no such header.

    NORM-10 sweep — two flat bodies must become OpenAI-shaped: (1) the `/internal/providers/status` disabled branch at line ~138 (`new Response('Not found', { status: 404 })`) and (2) the final catch-all at line ~392. Replace both with `withRequestId(openaiError('The requested endpoint does not exist.', 'invalid_request_error', 'not_found', null, 404))`.

    D-07 — upstream error passthrough with model-ID de-leak:
    - routing/cooldown-manager.ts: widen classifyError's return type with `message: string | undefined`; for `err instanceof GroqAPIError || err instanceof CerebrasAPIError` return `err.message`; for the unknown-error fallback return undefined. Update the return type annotation accordingly.
    - model-registry.ts: add `export function rewriteUpstreamModelIds(text: string): string` that, for every alias from listAliases() and every provider mapping of that alias, replaces all occurrences of the upstream model ID in `text` with the alias (use `split(upstreamId).join(alias)` to avoid regex-escaping issues). Iterate longer upstream IDs first is unnecessary for the current registry but order by descending upstream-ID length for safety.
    - index.ts: in BOTH non-failover error returns (streaming branch ~line 244 and non-streaming branch ~line 349), replace the hardcoded `'Upstream provider rejected the request.'` message with `rewriteUpstreamModelIds(classified.message ?? 'Upstream provider rejected the request.')`. Keep type 'invalid_request_error', code 'upstream_error', and the classified status fallback 502 unchanged. Never include classified.headers or the raw err object in the response or logs.
  </action>
  <verify>
    <automated>bun test && grep -v '^\s*//' index.ts | grep -c "new Response('Not found'" | grep -qx 0 && echo "NORM-10 sweep clean"</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains `crypto.randomUUID()` and a `withRequestId(` helper applied at every non-streaming return site (audit: every `return ` of a Response in the fetch handler is either wrapped or is the streaming Response whose constructor headers include 'X-Request-ID')
    - `grep -v '^\s*//' index.ts | grep -c "new Response('Not found'"` outputs 0
    - index.ts streaming Response headers object contains `'X-Request-ID': requestId`
    - index.ts contains `X-LLM-Provider` gated on `config.exposeProviderHeader`
    - routing/cooldown-manager.ts classifyError return includes `message:` and returns `err.message` for APIError instances
    - model-registry.ts contains `export function rewriteUpstreamModelIds(`
    - index.ts contains `rewriteUpstreamModelIds(classified.message` in both upstream-error paths
    - `bun test` exits 0
  </acceptance_criteria>
  <done>Every response carries X-Request-ID; all error bodies are OpenAI-shaped; upstream error text reaches clients without raw provider model IDs; provider header is opt-in only.</done>
</task>

<task type="auto">
  <name>Task 3: Structured per-request JSON log with LOG_LEVEL gating, requestId on events, D-08 usage-missing warn</name>
  <files>index.ts</files>
  <read_first>index.ts, .planning/phases/03-full-compliance-tests/03-RESEARCH.md</read_first>
  <action>
    Add a module-level `log(level: 'info' | 'warn' | 'error', data: Record<string, unknown>): void` helper gated by `config.logLevel` (numeric map error:0, warn:1, info:2; emit when the entry level <= configured level; emit via `console.log(JSON.stringify({ level, ...data }))`). This is the discretionary minimal-logger option from RESEARCH Pattern 4 — no separate logger.ts file.

    OBS-02 — one request-completion log line for POST /v1/chat/completions. Capture `const requestStart = Date.now();` at the top of the fetch handler. Track during the provider loop: `attempt` (1-based counter), `failoverReason` (string like 'status_429' or 'status_500' set when a failover occurs, else null), `chosenProvider`, and `upstreamModelId`. Emit fields: requestId, timestamp (new Date(requestStart).toISOString()), route (`${request.method} ${pathname}`), logicalAlias (input.model), provider, upstreamModelId, attempt, streaming (input.stream === true), statusCode, latencyMs (Date.now() - requestStart), failoverReason, usage (token-count object only — safe per RESEARCH).
    - Non-streaming: emit just before returning the success Response (statusCode 200) and in the exhaustion path (statusCode 503, provider null).
    - Streaming: emit inside the relay generator after the `data: [DONE]` yield (and in its catch path) so latencyMs is total stream duration — this is the chosen latency definition (Claude's discretion per CONTEXT: total duration, not TTFB); statusCode 200.

    OBS-03 — add `requestId` to the three existing event logs: provider_cooldown (2 sites), provider_failover (2 sites), stream_error_before_first_chunk (1 site). Convert them to the new log() helper (provider_cooldown/provider_failover at 'warn', request-completion at 'info'). Set failoverReason at these sites.

    D-08 warn — in the non-streaming success path, before calling normalizeResponse: if `result.usage === undefined` emit `log('warn', { event: 'usage_missing', provider, requestId })`. Never include the result object, params, or messages in this entry.

    OBS-04 redaction audit — verify no log call site references: request.headers.get('Authorization'), config.cerebrasApiKey/groqApiKey/personalProxyApiKey, input.messages, any choices content, any field named reasoning, or classified.headers/err.headers. The existing log sites already comply; keep it that way in the new ones.
  </action>
  <verify>
    <automated>bun test && grep -c "usage_missing" index.ts | grep -qx 1 && grep -v '^\s*//' index.ts | grep -c "input.messages" | grep -qx 0 && echo "logging redaction gates pass"</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains `function log(` with LOG_LEVEL gating reading config.logLevel
    - index.ts contains a request-completion log entry with keys requestId, route, logicalAlias, provider, upstreamModelId, attempt, streaming, statusCode, latencyMs, failoverReason
    - provider_cooldown, provider_failover, and stream_error_before_first_chunk log objects each include requestId
    - index.ts contains exactly one `usage_missing` warn site that logs only event/provider/requestId
    - `grep -v '^\s*//' index.ts | grep -c 'input.messages'` outputs 0 (messages are referenced only via the params construction, which is not a log site — if the params construction uses input.messages, scope the grep to console.log/log( lines instead and document the adjusted gate in the summary)
    - `bun test` exits 0
  </acceptance_criteria>
  <done>Every chat completion produces one OBS-02-complete log line; cooldown/failover events are linkable via requestId; missing upstream usage is visible in logs; nothing sensitive is ever logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → proxy | Untrusted Authorization headers and bodies enter here |
| upstream error → client | Provider error text crosses into client-visible bodies (D-07) |
| proxy → stdout logs | Request metadata crosses into log aggregation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | log() call sites | mitigate | OBS-04 redaction: never log Authorization, keys, messages, content, reasoning, or err.headers; grep gate in Task 3 verify |
| T-03-04 | Information Disclosure | D-07 error passthrough | mitigate | rewriteUpstreamModelIds() de-leaks provider model IDs from error text before client sees it; classified.headers never forwarded |
| T-03-05 | Spoofing | X-Request-ID | accept | UUID is correlation metadata, not an auth token; generated server-side per request (inbound X-Request-ID not honored — discretion exercised: always generate fresh, simplest and unspoofable) |
| T-03-06 | Information Disclosure | X-LLM-Provider header | mitigate | Gated behind EXPOSE_PROVIDER_HEADER (default false); provider topology hidden by default |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 3 |
</threat_model>

<verification>
- `bun test` green
- `bun -e "await import('./index.ts')"` exits 0 silently (no server boot on import)
- Manual smoke (optional, real keys): `curl -i http://localhost:3000/health` shows X-Request-ID; `curl -i http://localhost:3000/nope` returns 404 JSON `{ "error": { ... } }` with X-Request-ID
</verification>

<success_criteria>
- OBS-01..05 and NORM-10 fully implemented; D-02 and D-07 honored
- createServer seam ready for Plan 03-03 integration tests
- No secret, prompt, or reasoning content reachable from any log or response path
</success_criteria>

<output>
Create `.planning/phases/03-full-compliance-tests/03-02-SUMMARY.md` when done
</output>
