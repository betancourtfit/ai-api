---
phase: 03-full-compliance-tests
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - tests/integration/mock-adapters.ts
  - tests/integration/server.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-08, TEST-09, TEST-10, TEST-11, TEST-12]
tags: [integration-tests, bun-test, mock-adapters, setSystemTime, sse]

must_haves:
  truths:
    - "bun test passes with 12 new integration cases (TEST-01..12) and zero regressions in the pre-existing suite"
    - "Integration tests boot a real Bun.serve instance via createServer(mockAdapters, 0) and assert through real fetch() — provider SDKs never hit the network and no real API keys are required"
    - "Provider routing state is reset via resetForTesting() in beforeEach so every test starts from a clean cursor and no cooldowns"
    - "TEST-03 advances the clock with setSystemTime() to expire a cooldown deterministically and restores real time afterwards"
  artifacts:
    - path: "tests/integration/mock-adapters.ts"
      provides: "Scriptable ProviderAdapter mocks returning upstream model IDs, with per-test mockImplementationOnce control and a reset helper"
      exports: ["makeMockAdapter", "resetMockAdapter"]
    - path: "tests/integration/server.test.ts"
      provides: "12 integration cases TEST-01..12 over the full HTTP path including headers and SSE wire format"
      min_lines: 150
  key_links:
    - from: "tests/integration/server.test.ts"
      to: "index.ts"
      via: "createServer(mockAdapters, 0) — the D-02 seam"
      pattern: "createServer\\("
    - from: "tests/integration/server.test.ts"
      to: "routing/provider-state.ts"
      via: "resetForTesting() in beforeEach"
      pattern: "resetForTesting\\(\\)"
---

<objective>
Build the 12-case integration test suite (TEST-01..12) against a real `Bun.serve()` instance booted through `createServer` with scriptable mock adapters (D-01, D-02, D-04), using `setSystemTime()` for cooldown time travel (D-03) and `resetForTesting()` for state isolation. This is the final plan of v1 — after it, all 76 requirements are complete.

Purpose: Satisfies Phase 3 success criterion 3 ("bun test reports all 12 test cases passing") and CLAUDE.md §22 acceptance criterion 20.
Output: `tests/integration/server.test.ts` + `tests/integration/mock-adapters.ts`; full suite green.
</objective>

## Phase Goal

**As an** API consumer, **I want to** receive fully OpenAI-shaped, traceable responses from a proxy whose behavior is proven by a deterministic test suite, **so that** my existing OpenAI clients work unchanged and regressions are caught before they reach me.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-compliance-tests/03-CONTEXT.md
@.planning/phases/03-full-compliance-tests/03-RESEARCH.md
@.planning/phases/03-full-compliance-tests/03-01-SUMMARY.md
@.planning/phases/03-full-compliance-tests/03-02-SUMMARY.md
@tests/routing/cooldown-manager.test.ts
@tests/routing/provider-state.test.ts
</context>

<interfaces>
<!-- Contracts the tests build against. Extracted from types.ts and Plan 03-02 output. -->

```typescript
// types.ts
export interface ProviderAdapter {
    name: string;
    complete(upstreamModelId: string, params: CompletionParams): Promise<CompletionOutcome>;
    stream(upstreamModelId: string, params: CompletionParams, signal: AbortSignal): Promise<AsyncIterable<StreamChunk>>;
}
export interface CompletionOutcome { result: ChatCompletionResult; headers: Headers; }

// index.ts (created in Plan 03-02)
export function createServer(adapters: Record<Provider, ProviderAdapter>, port?: number): ReturnType<typeof Bun.serve>;

// routing/provider-state.ts (existing)
export function resetForTesting(): void;

// Verified APIError constructor shape (tests/routing/cooldown-manager.test.ts lines 118-129, passing):
new GroqAPIError(status, undefined, "boom", headers)
new CerebrasAPIError(status, undefined, "boom", headers as ConstructorParameters<typeof CerebrasAPIError>[3])
```

Environment: `bun test` auto-loads `.env.test`, which already defines test values for PERSONAL_PROXY_API_KEY, CEREBRAS_API_KEY, GROQ_API_KEY (D-04: never real keys). Registry default in config.ts maps `gpt-oss-120b-balanced` → cerebras `gpt-oss-120b`, groq `openai/gpt-oss-120b`. PROVIDER_ORDER default `cerebras,groq`; DEFAULT_COOLDOWN_SECONDS default 60; MAX_PROVIDER_ATTEMPTS_PER_REQUEST default 2; EXPOSE_PROVIDER_HEADER default false.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Mock adapter factory, server harness, and contract tests (TEST-06..09, TEST-11, TEST-12)</name>
  <files>tests/integration/mock-adapters.ts, tests/integration/server.test.ts</files>
  <read_first>types.ts, index.ts, tests/routing/provider-state.test.ts, tests/routing/cooldown-manager.test.ts, .planning/phases/03-full-compliance-tests/03-RESEARCH.md</read_first>
  <action>
    tests/integration/mock-adapters.ts — `export function makeMockAdapter(name: 'cerebras' | 'groq')` returning `ProviderAdapter & { completeMock: ReturnType<typeof mock>; streamMock: ReturnType<typeof mock> }` using `mock()` from 'bun:test'. Default complete implementation resolves a CompletionOutcome whose result carries the UPSTREAM model ID (`gpt-oss-120b` for cerebras, `openai/gpt-oss-120b` for groq — this is what proves NORM-01/TEST-11 end-to-end), `choices[0].message.content` set to the adapter name (so alternation is assertable from the response body), usage {prompt_tokens: 10, completion_tokens: 5, total_tokens: 15}, and `headers: new Headers()`. Default stream implementation resolves an async-iterable yielding three chunks (role delta, content delta with the adapter name, finish chunk with finish_reason 'stop'), each carrying the upstream model ID and object 'chat.completion.chunk'. Also `export function resetMockAdapter(adapter)` that calls mockReset() on completeMock/streamMock and re-installs the default implementations — needed because TEST-05 sets persistent mockImplementation overrides that must not leak into later tests.

    tests/integration/server.test.ts harness:
    - Read the proxy key from `process.env['PERSONAL_PROXY_API_KEY']` (loaded by bun test from .env.test) into a PROXY_KEY const — never hardcode and never read .env values into the plan or logs.
    - `beforeAll`: build mockCerebras/mockGroq via makeMockAdapter; `server = createServer({ cerebras: mockCerebras, groq: mockGroq }, 0)` (port 0 = OS-assigned; read back from server.port).
    - `afterAll`: `server.stop(true)`.
    - `beforeEach`: `resetForTesting()` (TEST-12) plus resetMockAdapter on both adapters.
    - `afterEach`: `setSystemTime()` no-arg to restore real time (guards against TEST-03 leakage even on failure).
    - URL helper returning `http://127.0.0.1:${server.port}${path}` (config hostname is 0.0.0.0 — use 127.0.0.1 for client fetches). A `post(body, headers?)` helper for chat completion requests with Authorization Bearer PROXY_KEY and Content-Type application/json keeps tests terse.

    Contract tests (each `test()` named with its TEST-ID):
    - TEST-06: request with no Authorization header → 401, body.error has message/type/code keys, and X-Request-ID header present; request with `Bearer wrong-key` → 401.
    - TEST-07: model 'does-not-exist' → 400, body.error.code === 'model_not_found', X-Request-ID present.
    - TEST-08: three sub-assertions — body with `logprobs: true` → 400; body with `n: 2` → 400; body with `messages: [{ role: 'user', content: 'hi', name: 'x' }]` → 400. All bodies error-shaped.
    - TEST-09: valid non-streaming request → 200; body.object === 'chat.completion'; body.id, body.created present; body.choices[0].message.content is a string; body.usage.prompt_tokens/completion_tokens/total_tokens are numbers; body.time_info, body.x_groq, body.usage_breakdown, body.choices[0].message.reasoning all undefined; X-Request-ID header truthy; X-LLM-Provider header null (EXPOSE_PROVIDER_HEADER default false).
    - TEST-11: same valid request → body.model === 'gpt-oss-120b-balanced' even though the mock returned an upstream ID.
    - TEST-12: explicit determinism proof — after beforeEach reset, a single request always lands on cerebras first (mockCerebras.completeMock called exactly once, mockGroq.completeMock zero times); run the identical assertion in a second test() to prove cursor state does not leak between tests.

    Follow existing conventions: 4-space indent, `import type` for type-only imports, named bun:test imports (describe, test, expect, beforeAll, afterAll, beforeEach, afterEach, mock, setSystemTime).
  </action>
  <verify>
    <automated>bun test tests/integration/</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/mock-adapters.ts contains `export function makeMockAdapter(` and `export function resetMockAdapter(`
    - tests/integration/server.test.ts contains `createServer(` with port argument 0 and `server.stop(true)`
    - beforeEach calls `resetForTesting()` and afterEach calls `setSystemTime()` with no arguments
    - Tests named TEST-06, TEST-07, TEST-08, TEST-09, TEST-11, TEST-12 all pass via `bun test tests/integration/`
    - No test reads CEREBRAS_API_KEY/GROQ_API_KEY values or makes real network calls (mock adapters only; suite passes offline)
  </acceptance_criteria>
  <done>Harness boots the real server on a random port with mocks; auth, validation, shape, model-rewrite, and state-isolation cases pass deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Routing and streaming integration tests (TEST-01..05, TEST-10)</name>
  <files>tests/integration/server.test.ts</files>
  <read_first>tests/integration/server.test.ts, tests/integration/mock-adapters.ts, routing/provider-state.ts, routing/cooldown-manager.ts, .planning/phases/03-full-compliance-tests/03-RESEARCH.md</read_first>
  <action>
    Append the routing and streaming cases to tests/integration/server.test.ts. Throw real SDK error instances using the verified constructor shapes from tests/routing/cooldown-manager.test.ts: `new CerebrasAPIError(status, undefined, 'boom', headers)` for the cerebras mock, `new GroqAPIError(status, undefined, 'boom', headers)` for the groq mock.

    - TEST-01 (alternation): two consecutive valid requests after reset → first response body content 'cerebras', second 'groq' (or assert via completeMock call counts: cerebras 1 / groq 1 after two requests, cerebras 2 / groq 1 after a third when cursor wraps with both eligible — PROVIDER_ORDER is cerebras,groq).
    - TEST-02 (429 cooldown): `mockCerebras.completeMock.mockImplementationOnce` throws CerebrasAPIError(429, undefined, 'rate limited', new Headers({ 'retry-after': '30' })). Request 1 → 200 served by groq via failover (groq content). Request 2 → still groq (cerebras in cooldown: max(30, 60) = 60s default). Assert cerebras completeMock called exactly once across both requests.
    - TEST-03 (recovery, D-03): trigger the same 429 cooldown on cerebras; capture `const before = Date.now()` beforehand; after the failover request, `setSystemTime(new Date(before + 61_000))`; issue another request and assert cerebras receives a call again (back in rotation per RL-05). Real time is restored by the harness afterEach.
    - TEST-04 (transient failover): mockCerebras.completeMock.mockImplementationOnce throws CerebrasAPIError(500, undefined, 'boom', undefined) → request returns 200 with body.model === 'gpt-oss-120b-balanced' and groq content; client never sees the 500.
    - TEST-05 (exhaustion): persistent overrides — both completeMocks `mockImplementation` always throw their SDK's APIError(500, ...) → response status 503; body.error.code === 'no_provider_available'; body.error has message/type/code/param keys; X-Request-ID present. (resetMockAdapter in beforeEach restores defaults for subsequent tests.)
    - TEST-10 (SSE): valid request with `stream: true` → status 200; Content-Type header contains 'text/event-stream'; X-Request-ID header truthy; `const text = await res.text()` (buffering is fine in tests per RESEARCH Pattern 7); split on newlines filtering lines starting with 'data: '; last data line is exactly 'data: [DONE]'; at least one JSON chunk exists before it; parse the first chunk: object === 'chat.completion.chunk', model === 'gpt-oss-120b-balanced' (NORM-02), choices[0].delta.reasoning undefined (NORM-06).

    Finish by running the FULL suite to confirm 12 integration cases plus all pre-existing unit tests pass together (existing 39 + Plan 03-01 normalizer units + these 12).
  </action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - Tests named TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-10 all present and passing
    - TEST-03 contains a `setSystemTime(new Date(` call advancing past the cooldown and relies on the harness afterEach reset
    - TEST-05 asserts status 503 and body.error.code === 'no_provider_available'
    - TEST-10 asserts the final data line is exactly 'data: [DONE]' and a parsed chunk has object 'chat.completion.chunk' and model 'gpt-oss-120b-balanced'
    - `bun test` exits 0 with 0 failures across the entire repository
  </acceptance_criteria>
  <done>All 12 integration cases (TEST-01..12) pass alongside the existing suite; routing, cooldown, recovery, failover, exhaustion, and SSE wire format are proven deterministically with no network access.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env → suite | .env.test values feed config; must be test-only values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | test suite / .env.test | mitigate | D-04: suite reads PERSONAL_PROXY_API_KEY from env only for Bearer headers in local fetches; provider keys never used (mocked adapters); no key values committed to test files |
| T-03-08 | Denial of Service | test server lifecycle | mitigate | port 0 + server.stop(true) in afterAll prevents port squatting and hung handles |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 3 |
</threat_model>

<verification>
- `bun test` exits 0; output includes 12 integration tests named TEST-01..TEST-12
- Suite passes with networking disabled (no real provider calls) — D-04 holds
- Phase gate: total test count ≥ 51 (39 pre-existing + normalizer units + 12 integration)
</verification>

<success_criteria>
- TEST-01..12 implemented and green via the full HTTP path (real server, real fetch, mocked adapters)
- State isolation (resetForTesting + resetMockAdapter + setSystemTime restore) proven by deterministic re-runs
- Phase 3 success criterion 3 satisfied; all 76 v1 requirements complete
</success_criteria>

<output>
Create `.planning/phases/03-full-compliance-tests/03-03-SUMMARY.md` when done
</output>
