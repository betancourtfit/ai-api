---
phase: 02-routing-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.ts
  - routing/cooldown-manager.ts
  - routing/provider-state.ts
  - tests/routing/cooldown-manager.test.ts
  - tests/routing/provider-state.test.ts
  - types.ts
  - services/cerebras.ts
  - services/groq.ts
  - index.ts
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03, ROUTE-04, ROUTE-05, ROUTE-06, ROUTE-07, ROUTE-08, RL-01, RL-02, RL-03, RL-04, RL-05, RL-06, RL-07, EP-01, EP-03, EP-05, EP-06]
tags: [routing, round-robin, cooldown, failover, rate-limits, provider-state]

must_haves:
  truths:
    - "Two consecutive valid POST /v1/chat/completions requests are served by different providers when both are eligible (round-robin cursor advances once per request)"
    - "A 429 or 498 from a provider sets cooldownUntil = now + max(retryAfter, resetTokens, DEFAULT_COOLDOWN_SECONDS) and the request is retried on the alternate provider before any error reaches the client"
    - "A provider automatically re-enters rotation once Date.now() >= cooldownUntil with no manual intervention"
    - "A 400/401/403/404/413/422 from upstream is returned to the client immediately without failover"
    - "When no eligible provider exists, the proxy returns 503 with body { error: { message, type, code, param } }"
    - "GET /ready requires no auth and returns mode 'ok' when both providers eligible, 'degraded' when exactly one is, ready:false + 503 when none are"
    - "GET /internal/providers/status returns provider state JSON only with a valid Bearer key and only when ENABLE_INTERNAL_STATUS_ENDPOINT is true; no API key values appear in the response"
    - "GET /v1/models still returns only logical alias IDs (Phase 1 behavior unchanged)"
  artifacts:
    - path: "routing/provider-state.ts"
      provides: "ProviderState record, eligibility predicate, round-robin cursor, cooldown setters, resetForTesting (ROUTE-01..03, ROUTE-08, RL-05)"
      exports: ["isEligible", "chooseEligibleProviders", "advanceCursor", "setCooldown", "setRateLimitSnapshot", "recordSuccess", "recordFailure", "getStateSnapshot", "resetForTesting"]
    - path: "routing/cooldown-manager.ts"
      provides: "Provider-specific rate-limit header parsers, cooldown calculation, error classification (RL-01..04, ROUTE-05/06)"
      exports: ["parseCerebrasHeaders", "parseGroqHeaders", "calcCooldownMs", "classifyError"]
    - path: "tests/routing/provider-state.test.ts"
      provides: "Unit coverage: alternation, cooldown, recovery, exhaustion, reset"
    - path: "tests/routing/cooldown-manager.test.ts"
      provides: "Unit coverage: Groq duration strings, Cerebras float seconds, calcCooldownMs max-rule, classifyError status table"
    - path: "config.ts"
      provides: "4 new env vars: defaultCooldownSeconds, maxProviderAttemptsPerRequest, exposeProviderHeader, enableInternalStatusEndpoint"
      contains: "defaultCooldownSeconds"
    - path: "index.ts"
      provides: "Router loop replacing Phase 1 first-eligible loop; /ready and /internal/providers/status routes"
      contains: "chooseEligibleProviders"
  key_links:
    - from: "index.ts"
      to: "routing/provider-state.ts"
      via: "chooseEligibleProviders / advanceCursor / setCooldown / recordSuccess"
      pattern: "chooseEligibleProviders\\("
    - from: "index.ts"
      to: "routing/cooldown-manager.ts"
      via: "classifyError on caught SDK errors; calcCooldownMs for 429/498"
      pattern: "classifyError\\("
    - from: "routing/provider-state.ts"
      to: "model-registry.ts"
      via: "resolveUpstreamModel inside isEligible"
      pattern: "resolveUpstreamModel\\("
    - from: "services/cerebras.ts"
      to: "index.ts"
      via: "complete() returns { result, headers } via .withResponse()"
      pattern: "withResponse\\(\\)"
---

<objective>
Replace the Phase 1 "first-eligible provider" loop with a stateful round-robin router: in-memory ProviderState, eligibility checks, cooldown on 429/498 calculated from provider rate-limit headers, failover on transient errors, 503 when both providers are out, plus the /ready and /internal/providers/status endpoints that surface this state.

Purpose: free-tier quota distribution across Cerebras and Groq — the core routing behavior of the proxy (ROADMAP Phase 2 success criteria 1, 2, 3, 5).
Output: routing/provider-state.ts, routing/cooldown-manager.ts (both unit-tested), config additions, rewired index.ts non-streaming path, two new endpoints.
</objective>

## Phase Goal

**As an** API consumer, **I want to** have my chat completion requests automatically routed across Cerebras and Groq with cooldown and failover, **so that** my requests keep succeeding even when one provider hits its free-tier limits.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-routing-streaming/02-RESEARCH.md
@.planning/phases/02-routing-streaming/02-PATTERNS.md
@CLAUDE.md

Source files (current state — read before modifying):
@config.ts
@index.ts
@types.ts
@model-registry.ts
@services/cerebras.ts
@services/groq.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from the Phase 1 codebase. -->

From types.ts:
```typescript
export interface CompletionParams {
  messages: Array<{ role: "user" | "assistant" | "system"; content: string }>;
  temperature?: number | null;
  top_p?: number | null;
  max_completion_tokens: number;
  stop?: string | string[] | null;
  seed?: number | null;
}
export interface ChatCompletionResult { id; object: "chat.completion"; created; model; choices; usage; system_fingerprint? }
export interface ProviderAdapter {
  name: string;
  complete(upstreamModelId: string, params: CompletionParams): Promise<ChatCompletionResult>; // ← this plan changes the return type
}
```

From model-registry.ts:
```typescript
export function resolveUpstreamModel(alias: string, provider: string): string | undefined;
export function isKnownAlias(alias: string): boolean;
export function listAliases(): string[];
```

From index.ts (reuse, do not reimplement):
```typescript
function openaiError(message: string, type: string, code: string | number, param: string | null = null, status: number = 400): Response;
function extractBearerToken(request: Request): string | null;
function verifyToken(token: string, expected: string): boolean;
const adapterMap = { cerebras: cerebrasAdapter, groq: groqAdapter } as const;
```

From config.ts:
```typescript
export const config = { port, hostname, personalProxyApiKey, cerebrasApiKey, groqApiKey,
  cerebrasBaseUrl, groqBaseUrl, cerebrasVersionPatch, defaultMaxCompletionTokens,
  providerOrder /* Array<"cerebras"|"groq">, default ["cerebras","groq"] */,
  modelRegistryJson, logLevel } as const;
```

SDK facts (verified in 02-RESEARCH.md from node_modules source):
- Both SDKs: caught errors are `APIError` instances with `.status: number | undefined` and `.headers: Headers | undefined`. Import: `import { APIError as GroqAPIError } from 'groq-sdk'` and `import { APIError as CerebrasAPIError } from '@cerebras/cerebras_cloud_sdk'`.
- Both SDKs: `create(...).withResponse()` resolves `{ data, response }` where `response.headers` is a Web `Headers`.
- Groq 498 falls through to generic `APIError` (no named subclass) — check `error.status === 498`.
- Cerebras reset headers are RELATIVE float seconds (e.g. "11.38") — `cooldownUntil = Date.now() + parseFloat(v) * 1000` (Pitfall 2).
- Groq reset headers are duration strings ("2m59.56s", "30s", "1m") — regex `^(?:(\d+)m)?(?:([0-9.]+)s)?$` with both groups optional (Pitfall 3). `retry-after` is plain integer seconds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cooldown manager — header parsers, cooldown calc, error classification (TDD) + config env vars</name>
  <files>config.ts, routing/cooldown-manager.ts, tests/routing/cooldown-manager.test.ts</files>
  <read_first>
    - config.ts (append-only change; existing `Number(process.env[...] ?? default)` and required() patterns)
    - .planning/phases/02-routing-streaming/02-RESEARCH.md §Architecture Patterns Pattern 2 and Pattern 3 (full reference implementations for parsers, calcCooldownMs, classifyError)
    - .planning/phases/02-routing-streaming/02-PATTERNS.md §routing/cooldown-manager.ts (export shape, private-helper, null-coalescing conventions)
    - request-schema.ts (analog for pure-function module style: comment header, exported types + functions, private helpers)
  </read_first>
  <behavior>
    tests/routing/cooldown-manager.test.ts (bun:test, describe/test/expect pattern from request-schema.test.ts; imports from '../../routing/cooldown-manager'):
    - parseGroqHeaders: "2m59.56s" → resetRequestsSeconds 179.56; "30s" → 30; "1m" → 60; "1m30s" → 90; retry-after "2" → retryAfterSeconds 2; absent headers → undefined fields (RL-02)
    - parseCerebrasHeaders: "33011.382867097855" → resetRequestsDaySeconds ≈ 33011.382867097855 (parseFloat, not parseInt); "11.382867097854614" → resetTokensMinuteSeconds ≈ 11.38; absent headers → undefined (RL-01)
    - calcCooldownMs: with retryAfterSeconds 2, resetTokensSeconds 179.56, default 60 → 179560 ms (max rule, RL-04); with no parsed values and default 60 → 60000 ms
    - classifyError: each of 408, 429, 498, 500, 502, 503, 504 → shouldFailover true (ROUTE-05, RL-06); each of 400, 401, 403, 404, 413, 422 → shouldFailover false (ROUTE-06); status undefined (connection error) → true; unknown status (e.g. 418) → true; non-APIError unknown object → { shouldFailover: true, status: undefined }
  </behavior>
  <action>
    RED: write the failing test file first, run it (module not found / failures expected), commit `test(02-01): add failing tests for cooldown manager`.

    GREEN: create routing/cooldown-manager.ts with comment header `// routing/cooldown-manager.ts — rate-limit header parsers, cooldown calc, error classification (RL-01..04, ROUTE-05/06)`. Named exports only, 4-space indent, `import type` discipline (verbatimModuleSyntax):
    - `export interface ParsedCerebrasHeaders { remainingRequestsDay?, remainingTokensMinute?, resetRequestsDaySeconds?, resetTokensMinuteSeconds? }` (all `number | undefined`)
    - `export interface ParsedGroqHeaders { remainingRequests?, remainingTokens?, resetRequestsSeconds?, resetTokensSeconds?, retryAfterSeconds? }`
    - `export function parseCerebrasHeaders(headers: Headers): ParsedCerebrasHeaders` — reads `x-ratelimit-remaining-requests-day`, `x-ratelimit-remaining-tokens-minute`, `x-ratelimit-reset-requests-day`, `x-ratelimit-reset-tokens-minute` via parseFloat (RL-01)
    - `export function parseGroqHeaders(headers: Headers): ParsedGroqHeaders` — reads `x-ratelimit-remaining-requests`, `x-ratelimit-remaining-tokens`, `x-ratelimit-reset-requests`, `x-ratelimit-reset-tokens`, `retry-after`; duration strings parsed with private `parseDuration()` using regex `^(?:(\d+)m)?(?:([0-9.]+)s)?$` (RL-02, RL-03 — separate parser functions, no shared generic parser)
    - `export function calcCooldownMs(parsed, defaultCooldownSeconds: number): number` — returns `Math.max(default, retryAfterSeconds?, resetTokensMinuteSeconds?, resetTokensSeconds?) * 1000` (RL-04); takes default as a parameter so the module stays pure (no config import)
    - `export function classifyError(err: unknown): { shouldFailover: boolean; status: number | undefined; headers: Headers | undefined }` — instanceof checks against GroqAPIError and CerebrasAPIError (value imports); `FAILOVER_STATUSES = new Set([408, 429, 498, 500, 502, 503, 504])`, `NO_FAILOVER_STATUSES = new Set([400, 401, 403, 404, 413, 422])`; undefined status or unknown status → failover true. NOTE: classifyError lives here (not index.ts as 02-PATTERNS sketched) so ROUTE-05/06 are unit-testable without importing index.ts, which calls Bun.serve() at module load (RESEARCH Pitfall 7).
    - Private helpers `toFloat`, `toNum`, `parseDuration` — not exported.

    Also append to config.ts before the closing `} as const` (follow existing patterns exactly):
    - `defaultCooldownSeconds: Number(process.env["DEFAULT_COOLDOWN_SECONDS"] ?? 60),`
    - `maxProviderAttemptsPerRequest: Number(process.env["MAX_PROVIDER_ATTEMPTS_PER_REQUEST"] ?? 2),`
    - `exposeProviderHeader: (process.env["EXPOSE_PROVIDER_HEADER"] ?? "false") === "true",`
    - `enableInternalStatusEndpoint: (process.env["ENABLE_INTERNAL_STATUS_ENDPOINT"] ?? "true") === "true",`

    Run tests with `bun --env-file=.env.test test tests/routing/cooldown-manager.test.ts` (config.ts required() throws without env vars — Phase 1 lesson). Commit `feat(02-01): cooldown manager — parsers, calcCooldownMs, classifyError + config vars`.
  </action>
  <verify>
    <automated>bun --env-file=.env.test test tests/routing/cooldown-manager.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `bun --env-file=.env.test test tests/routing/cooldown-manager.test.ts` exits 0 with all tests passing
    - routing/cooldown-manager.ts contains `export function parseCerebrasHeaders(`, `export function parseGroqHeaders(`, `export function calcCooldownMs(`, `export function classifyError(`
    - config.ts contains `defaultCooldownSeconds`, `maxProviderAttemptsPerRequest`, `exposeProviderHeader`, `enableInternalStatusEndpoint`
    - routing/cooldown-manager.ts does NOT import from './config' or '../config' (pure module: `grep -c "from '\.\./config'" routing/cooldown-manager.ts` returns 0)
    - Test asserts parseDuration handles "30s" and "1m" forms (test file contains the strings "30s" and "1m")
    - `bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Cooldown manager unit-tested for both header formats, max-rule cooldown, and the full ROUTE-05/06 status table; config exposes the 4 new env vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provider state — eligibility, round-robin cursor, cooldown lifecycle, resetForTesting (TDD)</name>
  <files>routing/provider-state.ts, tests/routing/provider-state.test.ts</files>
  <read_first>
    - .planning/phases/02-routing-streaming/02-RESEARCH.md §Architecture Patterns Pattern 1 (full reference implementation of the state module)
    - .planning/phases/02-routing-streaming/02-PATTERNS.md §routing/provider-state.ts (module-level mutable state pattern, named-export convention, no `as const` on state)
    - model-registry.ts (resolveUpstreamModel signature used by isEligible)
    - config.ts (providerOrder consumed for cursor rotation)
    - request-schema.test.ts (bun:test describe/test structure to replicate; add beforeEach)
  </read_first>
  <behavior>
    tests/routing/provider-state.test.ts (imports from '../../routing/provider-state'; `beforeEach(() => resetForTesting())` at describe level; uses alias "gpt-oss-120b-balanced" which the default MODEL_REGISTRY_JSON maps to both providers):
    - Initial chooseEligibleProviders("gpt-oss-120b-balanced") returns ["cerebras", "groq"] (default PROVIDER_ORDER, cursor 0)
    - After advanceCursor(), chooseEligibleProviders returns ["groq", "cerebras"] — alternation (ROUTE-03)
    - setCooldown("groq", Date.now() + 60000) → isEligible("groq", alias) false; chooseEligibleProviders returns ["cerebras"] only (ROUTE-02)
    - setCooldown("groq", Date.now() - 1) → isEligible("groq", alias) true — automatic recovery, no reset call needed (RL-05)
    - Both providers in future cooldown → chooseEligibleProviders returns [] (ROUTE-07 precondition)
    - isEligible("cerebras", "unknown-alias") false — alias has no mapping (ROUTE-02)
    - recordFailure("cerebras", 500) → consecutiveFailures 1, lastStatusCode 500, lastFailureAt set; recordSuccess("cerebras", 200) → consecutiveFailures 0, healthy true, cooldownUntil null (ROUTE-01)
    - setRateLimitSnapshot("groq", { remainingRequests: "10" }) → getStateSnapshot().groq.rateLimitSnapshot.remainingRequests === "10"
    - getStateSnapshot() returns a deep clone: mutating the returned object does not change a subsequent snapshot
    - resetForTesting() restores both providers to initial state and cursor to 0: after advanceCursor + setCooldown + resetForTesting, chooseEligibleProviders returns ["cerebras", "groq"] (ROUTE-08)
  </behavior>
  <action>
    RED: write the failing test file, run, commit `test(02-01): add failing tests for provider state`.

    GREEN: create routing/provider-state.ts with comment header `// routing/provider-state.ts — ProviderState, eligibility, round-robin cursor, resetForTesting() (ROUTE-01..03, ROUTE-08, RL-05)`. Implement per RESEARCH Pattern 1:
    - `export type Provider = 'cerebras' | 'groq';`
    - `export interface ProviderState` with exactly the ROUTE-01 fields: provider, enabled, configured, healthy, cooldownUntil (epoch ms | null), lastSelectedAt, lastSuccessAt, lastFailureAt, lastStatusCode, consecutiveFailures, rateLimitSnapshot?
    - Module-level mutable `const state: Record<Provider, ProviderState>` (NOT `as const`) and `let roundRobinCursor = 0`
    - `export function isEligible(provider, logicalModel)` — configured && enabled && healthy-or-expired-cooldown && `Date.now() >= cooldownUntil` (when set) && `resolveUpstreamModel(logicalModel, provider)` truthy. IMPORTANT: an expired cooldown must make the provider eligible again even if `healthy` is still false from setCooldown — implement as: if cooldownUntil is set and Date.now() < cooldownUntil → false; otherwise do not let a stale healthy=false from a past cooldown block eligibility (clear cooldownUntil and restore healthy when expired, or check `cooldownUntil` before `healthy`). RL-05 test must pass.
    - `export function chooseEligibleProviders(logicalModel): Provider[]` — rotate config.providerOrder starting at roundRobinCursor, filter by isEligible, set lastSelectedAt on returned candidates' first entry is NOT required; keep it simple: filter only
    - `export function advanceCursor(): void` — modulo config.providerOrder.length; called exactly once per request by the caller (Pitfall 6 — never advance inside chooseEligibleProviders)
    - `export function setCooldown(provider, untilMs, snapshot?)` — sets cooldownUntil, healthy=false, optional rateLimitSnapshot
    - `export function setRateLimitSnapshot(provider, snapshot: Record<string, string>)` — success-path snapshot update (RL-01/02 "each response")
    - `export function recordSuccess(provider, statusCode)` / `export function recordFailure(provider, statusCode)` per RESEARCH Pattern 1
    - `export function getStateSnapshot(): Record<Provider, ProviderState>` — deep clone via structuredClone or JSON round-trip
    - `export function resetForTesting(): void` — rebuild both entries, cursor = 0 (ROUTE-08)
    Imports: `import { config } from '../config';` and `import { resolveUpstreamModel } from '../model-registry';` only. Named exports only, 4-space indent.

    Run `bun --env-file=.env.test test tests/routing/provider-state.test.ts`. Commit `feat(02-01): provider state module — eligibility, cursor, cooldown lifecycle`.
  </action>
  <verify>
    <automated>bun --env-file=.env.test test tests/routing/ && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `bun --env-file=.env.test test tests/routing/provider-state.test.ts` exits 0 with all tests passing
    - routing/provider-state.ts contains `export function resetForTesting(` (ROUTE-08), `export function chooseEligibleProviders(`, `export function setCooldown(`, `export function setRateLimitSnapshot(`
    - routing/provider-state.ts contains `cooldownUntil` and `consecutiveFailures` (ROUTE-01 fields)
    - tests/routing/provider-state.test.ts contains `beforeEach` calling `resetForTesting`
    - A test asserts recovery with an already-expired cooldown (`Date.now() - 1` appears in the test file)
    - `bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Provider state module passes unit tests for alternation, cooldown entry/exit, exhaustion, snapshot isolation, and reset; ready to wire into index.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire router into index.ts — failover loop, header capture via withResponse, /ready, /internal/providers/status</name>
  <files>index.ts, types.ts, services/cerebras.ts, services/groq.ts</files>
  <read_first>
    - index.ts (current Phase 1 for-loop at lines 133-154 to replace; openaiError/auth helpers/route ordering to preserve)
    - types.ts (ProviderAdapter.complete return type to change)
    - services/cerebras.ts and services/groq.ts (complete() bodies — switch to .withResponse())
    - .planning/phases/02-routing-streaming/02-PATTERNS.md §index.ts (imports block to extend, route ordering, response patterns)
    - .planning/phases/02-routing-streaming/02-RESEARCH.md §Architecture Patterns (router loop pseudocode, Pattern 6 ready/status endpoints, §Accessing Headers from SDKs)
  </read_first>
  <action>
    1. types.ts: change `ProviderAdapter.complete` return type to `Promise<CompletionOutcome>` and add `export interface CompletionOutcome { result: ChatCompletionResult; headers: Headers }`. Update the interface comment (streaming still deferred to plan 02-02).

    2. services/cerebras.ts: in complete(), call `.withResponse()` on the create() promise: `const { data, response } = await cerebras.chat.completions.create({...} as ChatCompletionCreateParamsNonStreaming, { headers: {...} }).withResponse();` then `const completion = data as ChatCompletion.ChatCompletionResponse;` and return `{ result: {...field-by-field as today...}, headers: response.headers }`. Keep maxRetries:0, version-patch header, field-by-field construction unchanged.

    3. services/groq.ts: same change — `.withResponse()`, return `{ result, headers: response.headers }`. No other changes.

    4. index.ts — replace the Phase 1 first-eligible loop (the block under the comment "Phase 1: pick first eligible provider") with the stateful router:
       - New imports: `import { chooseEligibleProviders, advanceCursor, setCooldown, setRateLimitSnapshot, recordSuccess, recordFailure, getStateSnapshot, isEligible } from './routing/provider-state';` and `import { parseCerebrasHeaders, parseGroqHeaders, calcCooldownMs, classifyError } from './routing/cooldown-manager';` and `import type { Provider } from './routing/provider-state';`
       - Router loop (EP-01, ROUTE-03/04/07): `const candidates = chooseEligibleProviders(input.model); advanceCursor();` (advance exactly once per request — Pitfall 6). If candidates empty → `openaiError('No eligible provider available for the requested model.', 'server_error', 'no_provider_available', 'model', 503)`. Then `for (const provider of candidates.slice(0, config.maxProviderAttemptsPerRequest))`: resolve upstreamModelId; `try { const { result, headers } = await adapter.complete(upstreamModelId, params); }`:
         - On success: parse headers with the provider-matched parser (parseCerebrasHeaders for 'cerebras', parseGroqHeaders for 'groq'); build a `Record<string,string>` snapshot from the defined parsed fields (String() each) and call `setRateLimitSnapshot(provider, snapshot)` (RL-01/02 success path); `recordSuccess(provider, 200)`; rewrite `result.model = input.model`; return 200 JSON response (existing two-arg `new Response` pattern).
         - On catch: `const classified = classifyError(err);` `recordFailure(provider, classified.status ?? 0);`
           - If `!classified.shouldFailover` (ROUTE-06): return `openaiError('Upstream provider rejected the request.', 'invalid_request_error', 'upstream_error', null, classified.status ?? 502)` — do NOT echo upstream error bodies or headers.
           - If shouldFailover and status is 429 or 498 (RL-04, RL-06): parse `classified.headers` with the provider-matched parser (skip if headers undefined), `const cooldownMs = calcCooldownMs(parsed ?? {}, config.defaultCooldownSeconds);` `setCooldown(provider, Date.now() + cooldownMs, snapshot)`; log the cooldown event (RL-07) as one structured line: `console.log(JSON.stringify({ event: 'provider_cooldown', provider, status: classified.status, cooldownUntil: new Date(...).toISOString() }))` — never log raw headers, err objects, Authorization values, or request bodies.
           - For other failover statuses (408/5xx/undefined): log `{ event: 'provider_failover', provider, status }` and continue to next candidate — no cooldown (transient, ROUTE-05).
         - After the loop exhausts: return the same 503 no_provider_available error (ROUTE-07).
       - `GET /ready` route (EP-05): place it next to `/health`, BEFORE the auth gate (infrastructure-callable, RESEARCH Open Question 3). Compute `eligibleProviders` = (['cerebras','groq'] as Provider[]).filter(p => isEligible(p, listAliases()[0] ?? '')); `unavailableProviders` = the rest; `ready` = eligible.length > 0; `mode` = unavailable.length === 0 ? 'ok' : 'degraded'; respond `{ ready, mode, eligibleProviders, unavailableProviders }` with status 200 when ready, 503 otherwise.
       - `GET /internal/providers/status` route (EP-06): place AFTER the auth gate (auth required), guarded by `config.enableInternalStatusEndpoint` (when false, fall through to 404). Respond `{ providers: Object.values(getStateSnapshot()) }` as JSON 200. State contains only routing fields — no key material exists in ProviderState by construction.
       - `GET /v1/models` (EP-03): unchanged — verify it still returns listAliases() only.
    5. Run full suite + typecheck. Commit `feat(02-01): stateful round-robin router, cooldown failover, /ready + /internal/providers/status`.
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bun --env-file=.env.test test</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` exits 0
    - `bun --env-file=.env.test test` exits 0 (existing request-schema tests + new routing tests all pass)
    - index.ts contains `chooseEligibleProviders(` and exactly one call site of `advanceCursor()` in the chat-completions handler (`grep -c "advanceCursor()" index.ts` returns 1)
    - index.ts contains `'/ready'` and `'/internal/providers/status'`; the `/ready` branch appears BEFORE the `extractBearerToken` auth gate line and `/internal/providers/status` appears after it
    - index.ts contains `enableInternalStatusEndpoint`
    - index.ts contains `no_provider_available` and a 503 status for the exhaustion path
    - index.ts does not log raw headers or tokens: `grep -E "console\.(log|error)\(.*(headers|token|Authorization)" index.ts` returns no matches
    - services/cerebras.ts and services/groq.ts each contain `.withResponse()`
    - types.ts contains `export interface CompletionOutcome`
    - The Phase 1 comment "Phase 1: pick first eligible provider" no longer appears in index.ts
  </acceptance_criteria>
  <done>POST /v1/chat/completions routes through the stateful round-robin router with cooldown/failover; /ready reports ok/degraded; /internal/providers/status returns auth-gated state; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → proxy | Untrusted Bearer tokens and JSON bodies enter at Bun.serve fetch handler |
| proxy → providers | Proxy holds CEREBRAS_API_KEY / GROQ_API_KEY; upstream errors/headers flow back |
| proxy → logs | Structured log lines must never carry secrets or payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | GET /internal/providers/status | mitigate | Route placed after Bearer auth gate (constant-time verifyToken); gated by ENABLE_INTERNAL_STATUS_ENDPOINT; ProviderState contains only routing fields — no config/key values ever stored in state (Task 2/3) |
| T-02-02 | Information Disclosure | cooldown/failover logging | mitigate | Log only provider name, numeric status, ISO expiry via JSON.stringify of an explicit object; never log err objects, raw headers, Authorization, or bodies (Task 3 acceptance grep) |
| T-02-03 | Denial of Service | provider retry loop | mitigate | Attempts capped at config.maxProviderAttemptsPerRequest (default 2); 429/498 put the provider in cooldown instead of hot-retrying (Task 3) |
| T-02-04 | Spoofing | Bearer auth on status endpoint | mitigate | Reuses Phase 1 timingSafeEqual constant-time compare with length pre-check — no new auth path introduced |
| T-02-05 | Information Disclosure | GET /ready (unauthenticated) | accept | Exposes only provider eligibility names/booleans; required by load balancers/EasyPanel health checks; no secrets or quota numbers in body |
| T-02-06 | Information Disclosure | upstream error pass-through | mitigate | Non-failover upstream errors return a generic openaiError message; upstream error bodies/headers are never echoed downstream (Task 3) |

Supply chain: Phase 2 installs no new packages (RESEARCH §Package Legitimacy Audit — N/A); no install checkpoint required.
</threat_model>

<verification>
- `bun --env-file=.env.test test` — full suite green (cooldown-manager, provider-state, request-schema)
- `bunx tsc --noEmit` — zero errors
- Round-robin alternation, cooldown entry, automatic recovery, exhaustion-503 all asserted by unit tests against the pure routing modules (no live keys needed)
- Live smoke (optional, real .env): two consecutive `curl POST /v1/chat/completions` then `curl GET /internal/providers/status` shows both providers with lastSuccessAt set
</verification>

<success_criteria>
- ROADMAP Phase 2 criteria 1, 2, 3 and the /ready + status half of criterion 5 are demonstrably true via unit tests and endpoint behavior
- All 19 requirement IDs of this plan (ROUTE-01..08, RL-01..07, EP-01, EP-03, EP-05, EP-06) implemented and verifiable
- No secrets in logs, state snapshots, or downstream responses
</success_criteria>

<output>
Create `.planning/phases/02-routing-streaming/02-01-SUMMARY.md` when done
</output>
