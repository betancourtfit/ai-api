---
phase: 08-hexagonal-architecture-audit-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md
  - ARCHITECTURE.md
autonomous: true
requirements: [HEX-01, HEX-02, HEX-03]

must_haves:
  truths:
    - "08-ARCHITECTURE-AUDIT.md classifies every production .ts file (16 files) into exactly one hexagonal layer with its current LOC (HEX-01)"
    - "Every violation row carries a stable ID (V-NN), a severity (critical/high/medium/low), the rule it breaks, and at least one file:line citation that exists in the repo (HEX-02)"
    - "ARCHITECTURE.md states the per-layer import allowlist AND the explicit allowed/forbidden primitive lists for port signatures (HEX-03)"
    - "The audit records the measured test baseline (111 pass / 0 fail) and names it as the regression harness for plans 02-04"
    - "Zero source files are modified in this plan — it is documentation only; bun test count is unchanged"
  artifacts:
    - path: ".planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md"
      provides: "Per-file layer classification table + violation register V-01..V-15 with file:line evidence + ranked remediation order"
      contains: "V-01"
    - path: "ARCHITECTURE.md"
      provides: "Durable target-architecture contract: layer table, import rules, target tree, port primitive allow/deny lists"
      contains: "may not import"
  key_links:
    - from: "08-ARCHITECTURE-AUDIT.md violation register"
      to: "ARCHITECTURE.md layer import rules"
      via: "each V-NN row names the ARCHITECTURE.md rule it breaks, so the guard test in plan 02 has an executable target"
      pattern: "ARCHITECTURE\\.md"
    - from: "ARCHITECTURE.md target tree"
      to: "domain/ application/ adapters/ composition/ directories created in plans 02-04"
      via: "the tree is the single source of truth for every file path used by later plans"
      pattern: "domain/"
---

<objective>
Produce the two documents that make the rest of Phase 8 executable and reviewable: a **gap report** (`08-ARCHITECTURE-AUDIT.md`) that classifies every production file by layer and registers each boundary violation with `file:line` evidence, and a **durable target contract** (`ARCHITECTURE.md`) that states the import rules future phases must obey.

Purpose: ROADMAP scope items 1 and 2 are the audit and the gap report — they are deliverables in their own right, not planning scratch. They also convert an opinion ("this isn't hexagonal") into a checkable list that plans 02-04 close one by one and that the boundary-guard test can enforce mechanically.
Output: `.planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md` and repo-root `ARCHITECTURE.md`. **No `.ts` file is touched in this plan.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hexagonal-architecture-audit-refactor/08-RESEARCH.md

<interfaces>
<!-- Everything below is already researched in 08-RESEARCH.md — do not re-explore the codebase from scratch. -->

**Production files to classify (16, 1 355 LOC):**
`index.ts` (1012) · `routing/cooldown-manager.ts` (139) · `routing/provider-state.ts` (137) · `services/cerebras.ts` (122) · `services/groq.ts` (108) · `config.ts` (78) · `whisper-service.ts` (78) · `response-normalizer.ts` (78) · `types.ts` (60) · `audio-schema.ts` (60) · `request-schema.ts` (58) · `model-registry.ts` (49) · `schema-utils.ts` (11)
Test files (not classified as production, but listed under a "test coupling" heading): `tests/integration/server.test.ts` (688) · `tests/integration/gemini-compat.test.ts` (243) · `tests/integration/mock-adapters.ts` (89) · `tests/unit/response-normalizer.test.ts` (208) · `tests/unit/audio-schema.test.ts` (90) · `tests/routing/cooldown-manager.test.ts` (165) · `tests/routing/provider-state.test.ts` (87) · `tests/services/http-whisper-service.test.ts` (65) · `request-schema.test.ts` (174, at repo root).

**Violation register source of truth:** 08-RESEARCH.md §3 already contains V-01..V-15 with severities and citations. Reuse those IDs verbatim — later plans reference them by ID.

**Target layer rules:** 08-RESEARCH.md §5.1 table and §5.2 tree. Reuse verbatim.

**Measured baseline:** `bun test` → 111 pass / 0 fail / 287 expect() calls / 8 files. The ROADMAP's "104 existing tests" is stale; the audit must state the real number and say gates use `bun test` exit 0 with no decrease from the recorded count.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the verified baseline</name>
  <read_first>
    - package.json (scripts.test)
    - .planning/phases/08-hexagonal-architecture-audit-refactor/08-RESEARCH.md (§1 baseline facts)
  </read_first>
  <action>
    Run `bun test` and capture the exact summary line (pass count, fail count, expect() count, file count). Run `find . -name "*.ts" -not -path "./node_modules/*" -not -path "./dist/*" | xargs wc -l` and capture per-file LOC.

    Do not modify any file yet — this task only establishes the numbers that Task 2 writes down. If `bun test` does not report 0 failures, STOP and report: the phase cannot proceed without a green baseline, because every later plan uses "suite still green" as its safety gate.
  </action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun test` exits 0
    - The captured summary reports 0 failures and a pass count ≥ 111
    - Per-file LOC captured for all 13 production `.ts` files and all 9 test files
    - No file in the repository was modified by this task (`git status --porcelain` shows no new modifications beyond pre-existing ones)
  </acceptance_criteria>
  <done>The green baseline and LOC census are measured, so the audit states facts rather than estimates and later plans have a concrete regression target.</done>
</task>

<task type="auto">
  <name>Task 2: Write 08-ARCHITECTURE-AUDIT.md (gap report)</name>
  <read_first>
    - .planning/phases/08-hexagonal-architecture-audit-refactor/08-RESEARCH.md (§2 module map, §3 violation register, §4 what is already correct, §6 migration risk table)
    - index.ts (skim to confirm every cited line number still points at the cited construct)
    - routing/cooldown-manager.ts (lines 1-10, 61-88 — the SDK-import and classifyError citations for V-03)
    - routing/provider-state.ts (lines 1-30, 100-110 — module globals and resetForTesting citations for V-05)
    - types.ts (lines 31-55 — Headers and ProviderAdapter citations for V-04)
    - whisper-service.ts (lines 1-10, 31-45 — port+adapter-in-one-module citation for V-07)
  </read_first>
  <action>
    Create `.planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md` with these sections, in order:

    1. **Header** — phase, date, method ("static read of every production `.ts` file against the layer rules in `ARCHITECTURE.md`"), and the measured baseline from Task 1 (pass/fail/expect/file counts, total production LOC, and the note that ROADMAP's "104 tests" is stale).

    2. **Layer classification table** — one row per production file: `File | LOC | Current de-facto role | Target layer | Move required?`. Target layers must be exactly one of `domain`, `application`, `application/ports`, `adapters/inbound/http`, `adapters/outbound`, `composition`. Every one of the 13 production files appears exactly once (HEX-01).

    3. **Violation register** — a table with columns `ID | Severity | Violation | Evidence (file:line) | Boundary crossed | Closed by plan`. Copy V-01..V-15 from 08-RESEARCH.md §3 verbatim, preserving IDs and severities, and add the `Closed by plan` column with these assignments: V-03/V-04/V-05(partial)/V-07/V-13(partial) → plan 02; V-01/V-02/V-08/V-09/V-10/V-11/V-12/V-14 → plan 03; V-05(final)/V-06/V-13(final)/V-15 → plan 04. **Before writing each row, open the cited file and confirm the line number still points at the cited construct**; if a citation has drifted, correct the line number rather than the claim.

    4. **Severity counts** — a one-line rollup (2 critical, 5 high, 6 medium, 2 low, or the corrected figures if any citation was invalidated).

    5. **What is already correct** — the five items from 08-RESEARCH.md §4, stated as invariants the refactor must preserve (`ProviderAdapter` port, `WhisperService` port, `createServer()` seam, pure `response-normalizer.ts`, allowlist-rebuild normalization).

    6. **Test coupling** — the test-file table plus the exact import lines that pin production structure (`tests/integration/server.test.ts:8,9,10,13`; `tests/integration/gemini-compat.test.ts:21,22,23`; `request-schema.test.ts:4,5`; `tests/unit/response-normalizer.test.ts:5`; `tests/routing/*.test.ts`; `tests/services/http-whisper-service.test.ts:3`). State the shim-first rule: these imports stay untouched until plan 04.

    7. **Remediation order and risk** — the four-plan sequence with, for each, the violations closed and the single riskiest edit (from 08-RESEARCH.md §6 risk table).

    Write plain markdown tables. Do not invent violations that are not in the research register, and do not soften severities.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md && grep -c "^| V-" .planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-ARCHITECTURE-AUDIT.md` exists and contains all 7 numbered sections
    - The layer classification table contains exactly 13 production-file rows, and every row's Target layer is one of the 6 allowed values (HEX-01)
    - The violation register contains 15 rows with IDs `V-01`..`V-15`, each with a severity, a `file:line` citation, and a `Closed by plan` value in {02, 03, 04} (HEX-02)
    - Every `file:line` citation resolves: the named file exists and the cited line contains the cited construct
    - The document states the measured baseline (`bun test` pass/fail counts) and flags the ROADMAP's "104 tests" as stale
    - No `.ts` file was modified: `git status --porcelain -- '*.ts'` is empty
  </acceptance_criteria>
  <done>The gap report exists as a checkable artifact — every violation has an ID, an evidence citation, and an owning plan, so verification of plans 02-04 is a lookup rather than a judgement call.</done>
</task>

<task type="auto">
  <name>Task 3: Write ARCHITECTURE.md (durable target contract)</name>
  <read_first>
    - .planning/phases/08-hexagonal-architecture-audit-refactor/08-RESEARCH.md (§5.1 layer rules, §5.2 target tree, §5.3 de-vendoring seam, §7 boundary enforcement)
    - CLAUDE.md (§21 "Suggested architecture" — the target must not contradict the project's own stated module intent)
  </read_first>
  <action>
    Create repo-root `ARCHITECTURE.md` — the durable contract, written for a future contributor who has never read the phase docs. Sections:

    1. **Why hexagonal here** — two paragraphs max: the proxy has two interchangeable upstream providers, two transcription backends, and two wire contracts (OpenAI + Gemini); those are ports by nature. Cite the concrete pain: `index.ts` was 1 012 LOC with orchestration fused to `Bun.serve()`.

    2. **Layer table** — columns `Layer | Directory | May import | May NOT import`, filled exactly as in 08-RESEARCH.md §5.1 (Domain / Application / Ports / Adapters / Composition). The Domain row's "may not import" list must name: npm SDKs, `zod`, `Bun.*`, `Request`/`Response`/`Headers`, `process.env`, `config` (HEX-03).

    3. **Port signature primitives** — two explicit lists.
       Allowed: `AbortSignal`, `File`/`Blob`, `Uint8Array`, `AsyncIterable`, plain objects/records.
       Forbidden: `Headers`, `Request`, `Response`, `FormData`, every vendor SDK type (`groq-sdk`, `@cerebras/*`), `ZodType`.
       Include the one-sentence rationale: the allowed set is runtime-agnostic and models a real capability; the forbidden set is HTTP-transport- or vendor-specific (HEX-03, HEX-08).

    4. **Target tree** — the directory listing from 08-RESEARCH.md §5.2, verbatim, with the one-line purpose comment per module. Include the note that layers are top-level directories (no `src/`) because of the recorded project decision.

    5. **The de-vendoring seam** — the `toUpstreamFailure` (adapter) / `classifyUpstreamFailure` (domain) split from §5.3, including the failover status set `{408, 429, 498, 500, 502, 503, 504}` and the terminal set `{400, 401, 403, 404, 413, 422}`.

    6. **Enforcement** — state that `tests/architecture/boundaries.test.ts` is the executable form of this document and that adding a forbidden import fails `bun test`. Name the forbidden substrings the guard checks.

    7. **Invariants that outrank the architecture** — the public wire contract (paths, methods, status codes, header names, JSON shapes, SSE framing and the `data: [DONE]` sentinel), constant-time proxy-key comparison, never logging secrets/prompts/transcripts, and zero new npm packages. If a layering rule ever conflicts with one of these, the invariant wins.

    Keep it under ~150 lines. This file is read by every future phase; density beats completeness.
  </action>
  <verify>
    <automated>test -f ARCHITECTURE.md && grep -q "May NOT import" ARCHITECTURE.md && grep -q "boundaries.test.ts" ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `ARCHITECTURE.md` exists at repo root with all 7 sections
    - The layer table has 5 rows (Domain, Application, Ports, Adapters, Composition), each with a populated "May NOT import" cell (HEX-03)
    - The Domain "May NOT import" cell names all six of: npm SDK, `zod`, `Bun.`, `Response`/`Headers`, `process.env`, `config`
    - Both the allowed and forbidden port-primitive lists are present, and `Headers` appears in the forbidden list while `AbortSignal` appears in the allowed list (HEX-08)
    - The target tree names `domain/`, `application/ports/`, `application/use-cases/`, `adapters/inbound/http/`, `adapters/outbound/`, `composition/`
    - The failover status set `408, 429, 498, 500, 502, 503, 504` and terminal set `400, 401, 403, 404, 413, 422` both appear (HEX-05 target)
    - The invariants section names the wire contract, constant-time key comparison, no-secret-logging, and zero-new-dependencies
    - No `.ts` file was modified: `git status --porcelain -- '*.ts'` is empty
  </acceptance_criteria>
  <done>The target architecture is a written, citable contract at repo root — plans 02-04 implement against it and the boundary-guard test enforces it, so the rules survive past this phase.</done>
</task>

<task type="auto">
  <name>Task 4: Cross-link the contract and confirm zero code drift</name>
  <read_first>
    - ARCHITECTURE.md (as written in Task 3)
    - .planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md (as written in Task 2)
    - CLAUDE.md (locate §21 "Suggested architecture" and the "Working rules for the coding agent" list)
  </read_first>
  <action>
    In `08-ARCHITECTURE-AUDIT.md`, add a line under the header: `Target contract: see ARCHITECTURE.md (repo root).` In `ARCHITECTURE.md`, add a line under the header: `Gap report that motivated this: .planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md`.

    Then re-run the full suite and confirm this plan changed no behavior at all: `bun test` must report the same pass count as Task 1, and `git status --porcelain -- '*.ts'` must be empty (only the two markdown files are new/modified relative to the plan's start).

    Do NOT edit `CLAUDE.md` in this plan — the CLAUDE.md update happens in plan 04 once the tree actually exists, so the instructions never describe a layout that is not on disk.
  </action>
  <verify>
    <automated>bun test && test -z "$(git status --porcelain -- '*.ts')"</automated>
  </verify>
  <acceptance_criteria>
    - `08-ARCHITECTURE-AUDIT.md` contains the string `ARCHITECTURE.md` in a cross-reference line
    - `ARCHITECTURE.md` contains the string `08-ARCHITECTURE-AUDIT.md` in a cross-reference line
    - `bun test` exits 0 with the same pass count recorded in Task 1 (no test added, removed, or broken)
    - `git status --porcelain -- '*.ts'` produces no output — zero TypeScript files changed by this plan
    - `CLAUDE.md` is unmodified
  </acceptance_criteria>
  <done>The two documents reference each other and the codebase is provably untouched, so plan 02 starts from the same green baseline the audit recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan writes two markdown files. No code path, no request handling, no credential handling, and no dependency is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | audit document content | mitigate | The audit cites `file:line` and construct names only. It must NOT copy any value from `.env`/`.env.local`, any API key, or any real request/response payload into the document. Evidence is structural, never data. |
| T-08-01-02 | Tampering | documented invariants weakening later review | mitigate | `ARCHITECTURE.md` §7 records the security invariants (constant-time key comparison, no secret/prompt/transcript logging, request-size limits, wire contract) as outranking every layering rule, so plans 02-04 cannot trade a control for cleanliness. |
| T-08-01-03 | Tampering | npm/pip/cargo installs | accept | No dependency is added or changed in this plan — slopcheck N/A. |
</threat_model>

<verification>
- `bun test` exits 0 with pass count unchanged from the Task 1 baseline (≥ 111 pass, 0 fail).
- `git status --porcelain -- '*.ts'` is empty — this plan is documentation-only.
- `08-ARCHITECTURE-AUDIT.md` contains 13 production-file classification rows and 15 violation rows (`V-01`..`V-15`), each with a resolvable `file:line` citation and an owning plan.
- `ARCHITECTURE.md` contains the 5-row layer table, both port-primitive lists, the target tree, the de-vendoring seam, the enforcement note, and the invariants section.
</verification>

<success_criteria>
- Every production `.ts` file is classified into exactly one target layer, with the move (if any) named (HEX-01).
- Every boundary violation is registered with an ID, severity, `file:line` evidence, the rule it breaks, and the plan that closes it (HEX-02).
- `ARCHITECTURE.md` states, per layer, what may and may not be imported, plus the allowed/forbidden port-primitive lists (HEX-03).
- The codebase is byte-identical to the phase start; the test suite is green at the recorded baseline.
</success_criteria>

## Artifacts this phase produces

New symbols and files introduced by **this plan** (nothing here exists before it):

| Artifact | Kind | Path |
|---|---|---|
| Architecture gap report | markdown document | `.planning/phases/08-hexagonal-architecture-audit-refactor/08-ARCHITECTURE-AUDIT.md` |
| Target architecture contract | markdown document | `ARCHITECTURE.md` (repo root) |
| Violation IDs `V-01`..`V-15` | stable identifiers referenced by plans 02-04 | inside `08-ARCHITECTURE-AUDIT.md` |

No TypeScript symbol, file, or export is created, moved, or removed by this plan.

<output>
Create `.planning/phases/08-hexagonal-architecture-audit-refactor/08-01-SUMMARY.md` when done.
</output>
